# 01 — Functional Requirements

Priority: `M` must · `S` should · `K` could — Stage: 0 MVP · 1 core ·
2 identification · 3 ecosystem. Column meanings: [README](README.md).

---

## CORE — Items, type system, locations, tags

### Items

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-CORE-001 | An item is identified by a **UUIDv7** that may be generated by the client or the server. | M | 0 | Creation with a client-generated ID returns `201`; re-creating the same ID with the same content returns `200` |
| REQ-CORE-002 | An item has at least: name, description, kind (`PHYSICAL`/`DIGITAL`), type reference, quantity with unit. These are the columns `inventory.item` carries ([07 §7.3](../architecture/07-data-model.md)); everything else about an item is a **type field** (stage 1) or a **tag** ([04 `tagging`](../architecture/04-building-blocks.md)). | M | 0 | Creation without a name is rejected with `422`; the five named fields round-trip through the API with no type system present |
| REQ-CORE-003 | A **physical** item resides in exactly one location. | M | 0 | Creation without a location is rejected; enforced by a database constraint |
| REQ-CORE-004 | A **digital** item has no physical location but instead a carrier/account, licence key, expiry date and seat count. | M | 1 | A digital item is creatable without a location; the licence field is stored encrypted |
| REQ-CORE-005 | Items additionally carry the fields of their type version, validated against its JSON Schema. | M | 1 | An invalid attribute value → `422` with the field path |
| REQ-CORE-006 | Items can be related: `ACCESSORY_OF`, `PART_OF`, `REPLACEMENT_FOR`, `RELATED`. | S | 1 | A relation is creatable, visible from both sides, self-reference rejected |
| REQ-CORE-007 | An item can be a **bundle** containing other items without removing them from their location. | K | 1 | A bundle is creatable; a cycle is rejected |
| REQ-CORE-008 | Consumables have a quantity and a **minimum stock**; falling below it raises an event. | S | 1 | A quantity below the minimum produces `StockBelowMinimum` |
| REQ-CORE-009 | Deletion is **two-stage**: trash with restore, final removal as a separate operation. | M | 1 | A deleted item is fully restorable within the retention period |
| REQ-CORE-010 | Every change creates a **revision record**; earlier states are viewable and restorable. | M | 1 | The history shows before/after; a restore creates a new version |
| REQ-CORE-011 | Items can be changed in **bulk**: move, tag, change type, delete. | S | 1 | Up to 500 entries per call, partial success with a status line per entry |
| REQ-CORE-012 | An item can be saved as a **template** and used for new items. | K | 1 | Creating from a template adopts type, attributes and tags |
| REQ-CORE-013 | Attribute search is **transactionally exact** through `item_attr_index`, including during an OpenSearch outage. Setting `deleted_at` **removes** an item's rows from the side table in the same transaction, and restoring it rebuilds them: deletion is two-stage (`REQ-CORE-009`), so the table's `ON DELETE CASCADE` fires only on final removal and a trashed item would otherwise answer filters for the whole retention period ([ADR-0004](../adr/0004-attribute-storage-model.md), [07 §7.3](../architecture/07-data-model.md)). | M | 1 | A filter on a `searchable` attribute returns correct results without OpenSearch; an item moved to the trash **disappears from attribute filters, facet counts and reports immediately**, and reappears on restore |
| REQ-CORE-014 | Every item carries free-form notes with limited Markdown. | S | 1 | HTML in notes is stripped server-side |

> **`condition` left this row on 2026-09-11** (open point **O19**). It was
> required at **stage 0**, where the type system that would carry it does not
> exist, and it had no column in `inventory.item` — while the rest of the corpus
> already modelled it twice over: as a **tag group** *("condition" with
> `new`/`used`/`broken`, [04 `tagging`](../architecture/04-building-blocks.md))*
> and as a **JSONB attribute** in [07 §7.3](../architecture/07-data-model.md)'s
> own worked example. Three models for one field, none of them a stage-0 column.
> It stays where it already was, and stage 0 no longer promises it.
>
> The same pass corrected [04 Roadmap](04-roadmap-and-stages.md), which listed
> *note* and *purchase data* among stage 0's fixed fields although `REQ-CORE-014`
> and `REQ-LIFE-001` both say stage 1. The Stage column is the single source, and
> it now agrees with the DDL and with this row.


### Type system

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-CORE-020 | Item types are creatable and editable **in the running system**, without a release and without a restart. | M | 1 | A new type with fields in ≤ 2 min without developer involvement |
| REQ-CORE-021 | A type defines fields with: key, multilingual label, data type, required, default, unit, constraints, help text, group, order. | M | 1 | Every property settable in the UI and represented in the API |
| REQ-CORE-022 | Supported data types: `text`, `multiline`, `integer`, `decimal`, `money`, `boolean`, `date`, `datetime`, `enum`, `multi-enum`, `url`, `email`, `quantity`, `reference`, `secret`, `file`. | M | 1 | An acceptance test per data type for input, storage, validation and display |
| REQ-CORE-023 | Fields are individually markable as `searchable`, `sortable`, `facetable` and `sensitive`. | M | 1 | The marking takes effect on index, sorting, facets and encryption |
| REQ-CORE-024 | Types support **inheritance**; an inheriting type may tighten fields but not loosen them. | S | 1 | Loosening is rejected with an explanatory message |
| REQ-CORE-025 | Types are **versioned**; an item references a type version. | M | 1 | A type change devalues no existing items |
| REQ-CORE-026 | A field is deprecated and hidden; the values remain. Final deletion first shows the affected set. | M | 1 | The preview states the exact number of affected items |
| REQ-CORE-027 | A **JSON Schema (draft 2020-12)** is generated per type version and served through the API. | M | 1 | A client validates offline with the same result as the server |
| REQ-CORE-028 | Per-field visibility rules are possible as a **simple condition** over another field of the same item. | S | 1 | The condition is evaluable offline; no loops, no external access |
| REQ-CORE-029 | Value lists (`enum` sources) are independent, reusable objects. | S | 1 | One value list is usable in several types |
| REQ-CORE-030 | Shipped type templates: book, tool, appliance, furniture, clothing, software licence, document, food. | S | 1 | Templates are importable and fully editable |
| REQ-CORE-031 | Calculated fields with a formula language are **excluded**. | M | 1 | No formula language in the API; see [ADR-0020](../adr/0020-configuration-as-data.md) |
| REQ-CORE-032 | Field types that carry a unit alongside a number — `money` (ISO 4217 code) and `quantity` (unit) — project that unit into `item_attr_index.unit_value`. Any aggregation over such a field **groups by the unit**; a mixed-unit total is never produced. | M | 1 | A test with two currencies returns two rows, not one sum; the same for two units |

### Locations

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-CORE-040 | Locations form an **arbitrarily deep tree** (default ceiling 12 levels). | M | 0 | Building → room → shelf → box → compartment is creatable |
| REQ-CORE-041 | Locations have **categories with their own fields**, configurable in the running system. | M | 1 | A "moving box" category with target room, packing date and seal number is creatable |
| REQ-CORE-042 | Shipped categories: building, floor, room, furniture, shelf, compartment, drawer, box, moving box, vehicle, warehouse, outdoor storage, locker. | S | 1 | All present and editable |
| REQ-CORE-043 | A category can be marked **mobile**; a mobile location is moved as a whole without moving its contents individually. | M | 1 | Moving a box with 200 items produces **one** event, not 200 |
| REQ-CORE-044 | A location's full path is retrievable without recursion. | M | 0 | `ltree` path; subtree queries served by an index |
| REQ-CORE-045 | Cycles in the tree are impossible. | M | 0 | Moving a node under its own child is rejected |
| REQ-CORE-064 | **Names are unique among siblings**, case-insensitively, so that the tree a person reads is the tree the database holds. A deleted location leaves a tombstone and a tombstone does **not** block re-using its name. Written down 2026-09-12: the partial unique index `location_sibling_name` had enforced this since the first migration and no requirement stated it, so the API answered `500` for the case the schema was deliberately built to refuse. | M | 0 | Two siblings with the same name, and with names differing only in case, are both refused with `409` `name-taken`; the name of a deleted sibling is re-usable; a rename to a location's own name in different case succeeds |
| REQ-CORE-046 | A non-empty location is not deleted; it is emptied or archived. | M | 1 | The delete attempt states the number of contained items |
| REQ-CORE-047 | A category may restrict its permitted child categories (optional). | K | 1 | The rule is settable and effective |
| REQ-CORE-048 | A location is **sealable** (a moving box); changing its contents requires unsealing. | S | 2 | A sealed box rejects content changes with an explanatory message |
| REQ-CORE-049 | The items contained in a location are listable — either directly or including the subtree. | M | 0 | Both modes return correct counts |
| REQ-CORE-050 | Locations carry photos and attachments like items. | S | 1 | A photo of a room can be attached |

### Tags

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-CORE-060 | Tags are tenant-wide, freely creatable, and apply to items **and** locations. | M | 1 | A tag is assignable to both kinds of object |
| REQ-CORE-061 | Tags can be grouped (e.g. a group "condition"). | S | 1 | A group is creatable; the UI groups accordingly |
| REQ-CORE-062 | Tags have a colour and an icon. | K | 1 | Visible in lists and detail views |
| REQ-CORE-063 | Two tags can be merged; all assignments move with them. | S | 1 | The merge is one operation, traceable in the audit log |

---

## MED — Media and attachments

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-MED-001 | Photos and documents can be attached to items and locations. | M | 0 | Upload and display work |
| REQ-MED-002 | An item has a **primary image** shown in lists. | M | 0 | The primary image is selectable; the first image is the default |
| REQ-MED-003 | Permitted **upload** formats: JPEG, PNG, WebP, AVIF, PDF, TXT, CSV. HEIC is **accepted and transcoded on ingest**; it is never stored in its original form. **Every image is stored as AVIF** — not only HEIC — because one stored format is one decoder path and one metadata convention ([ADR-0052](../adr/0052-one-stored-image-format.md)). Documents are stored as they arrived. **SVG is rejected.** | M | 0 | Uploading an SVG file is rejected; an uploaded HEIC file is retrievable only as AVIF; no stored blob has a HEIC magic number |
| REQ-MED-004 | The type is detected from **magic bytes**; the extension and the reported MIME type are discarded. | M | 0 | A renamed executable is rejected |
| REQ-MED-005 | Images are **re-encoded** server-side; the derivatives `thumb` (200 px), `preview` (1024 px) and `full` (re-encoded, max 4096 px) are generated **asynchronously in the `worker`** ([04 §4.1](../architecture/04-building-blocks.md)) over the broker that [ADR-0051](../adr/0051-broker-in-stage-0.md) moved to this stage for exactly this reason. | M | 0 | All three derivatives exist once the pipeline completes, and a variant URL is offered only for a variant that exists — the same rule `REQ-MED-013` applies to the bytes themselves. It read *“present after upload”* until 2026-09-11, which named a moment asynchronous generation cannot promise and which stage 0 had **no mechanism** to reach at all: `api` and `worker` are separate processes and the broker was filed under stage 1 |
| REQ-MED-006 | **EXIF data including GPS is stripped.** Retention only on an explicit tenant setting. | M | 0 | A reference image contains no coordinates after upload |
| REQ-MED-007 | Blobs are **content-addressed within a tenant** (`sha256/<tenantId>/<hash>`); a tenant's duplicates are stored once. Deduplication **never** spans tenants ([ADR-0032](../adr/0032-per-tenant-blob-addressing.md)). | M | 0 | A second upload of the same file by the same tenant consumes no additional storage; the same file uploaded by a **second** tenant produces an independent object, is scanned in its own right, and reveals nothing about the first tenant holding it |
| REQ-MED-008 | Uploads are **resumable** (tus) and survive network changes. | M | 1 | An interrupted upload is continued, not restarted |
| REQ-MED-009 | Storage is replaceable through the `BlobStore` port. `filesystem` is an **in-core** adapter writing to the in-deployment **`blobstore` service**, which holds the `blobdata` volume so that `api` and `worker` stay stateless (`REQ-NFR-008`) and the default media store has a backup row at all ([ADR-0043](../adr/0043-blobstore-as-its-own-service.md)), speaks gRPC over mTLS and is written in Rust because that volume-holding service is budgeted at 128 MB ([ADR-0050](../adr/0050-blobstore-service-in-rust.md)); S3 and **Nextcloud (WebDAV)** ship as **first-party plugins**, because they open a connection outside the deployment ([ADR-0026](../adr/0026-core-outbound-via-plugins.md)). | M | 1 | All of them pass the same adapter test suite, in-core and plugin alike; a network test shows the core itself reaching neither S3 nor Nextcloud; a `minimal` stack stores a photo, is restarted, and still serves it — the case that had no storage location at all before 2026-09-11 |
| REQ-MED-010 | Media is served only through **signed, short-lived URLs**, from a dedicated hostname. The signature uses `HOMEINV_URL_SIGNING_KEY_FILE` (`REQ-SEC-106`), which is not the JWT signing key. | M | 0 | Direct access without a signature is rejected; an expired or foreign signature is rejected; a URL signed with the JWT key is rejected |
| REQ-MED-011 | A blob is removed only when no link remains — with a grace period. | M | 1 | Reference counting verified; the cleanup run removes only orphans |
| REQ-MED-012 | Photos can be taken offline and are uploaded later. | M | 3 | Capture in flight mode, upload after the network returns |
| REQ-MED-013 | **Every** upload is scanned for malware before release, **in the `worker`** ([ADR-0054](../adr/0054-the-scan-is-asynchronous.md)). The upload is answered `202` with a `Location`; until the verdict arrives the blob is `PENDING_SCAN`, carries no signed URL and is not retrievable. | M | 0 | No retrieval before the scan completes — proven on the serving path and not only by withholding the URL; see REQ-SEC-091 ff. |

---

## IDENT — Identification, codes, scanning

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-IDENT-001 | Every physical item can be given a **public short code**: Crockford Base32, **10 payload characters (50 bits) plus a Damm check symbol** from the same alphabet ([ADR-0030](../adr/0030-public-code-format.md)). | M | 2 | The code is generatable and globally unique; the check symbol rejects every single-character error **and** every adjacent transposition; the symbol is always one of the 32 alphabet characters; known test vectors reproduce the published quasigroup table |
| REQ-IDENT-002 | Codes can be generated and printed **in advance**, before the item exists. | M | 2 | 24 unassigned codes are generatable and printable |
| REQ-IDENT-003 | An unassigned code is bound to an item by scanning it. | M | 2 | A scan in `ASSIGN` mode binds the code |
| REQ-IDENT-004 | A code can be **reassigned**; the binding history is preserved. | S | 2 | The old binding is visible in the history |
| REQ-IDENT-005 | QR codes are generated and contain a URL plus the UUID in the fragment. | M | 2 | A scan with any camera app leads to login resp. the item |
| REQ-IDENT-006 | Scanning works in the browser through the camera: `BarcodeDetector` **where the browser implements it** — Chromium on Android, ChromeOS and macOS — and **ZXing-WASM everywhere else**. WebKit implements no `BarcodeDetector`, so on iPhone and iPad the WASM decoder is the **only** browser path, not a fallback ([10 §10.3](../architecture/10-identification-and-labels.md), risk R17). | M | 2 | Verified on Android/Chrome **and on iOS/Safari, where the WASM path is the one under test**. A measured scan-to-result on current iPhone hardware, held to the same < 1 s as scenario U3 — this row read "Verified on Android/Chrome and iOS/Safari" against an API Safari does not have |
| REQ-IDENT-007 | **HID handheld scanners** are recognised without installation and treated as a scan. | S | 2 | Input above 20 characters/s with a terminator counts as a scan |
| REQ-IDENT-008 | There is a **continuous scan mode** with audible feedback and debouncing (2 s per code). | M | 2 | 10 codes in a row without restarting the camera |
| REQ-IDENT-009 | Code formats are replaceable through the `CodeFormat` port; QR ships with the product. | M | 2 | DataMatrix is usable as a plugin without a core change |
| REQ-IDENT-010 | **Foreign codes** (EAN-13, EAN-8, UPC-A, ITF-14, ISBN, Code128) are recognised and bound as an `ExternalCode`. | M | 2 | The EAN of a retail product is recognised and assigned |
| REQ-IDENT-011 | An unknown scan offers to create a new item from it. | S | 2 | The flow takes ≤ 3 interactions to a saved record |
| REQ-IDENT-012 | **Scan sessions** with a mode (`LOOKUP`, `ASSIGN`, `MOVE`, `STOCKTAKE`, `CAPTURE`, `LEND`, `RETURN`) are maintained. | M | 2 | An acceptance test per mode |
| REQ-IDENT-013 | Scan sessions run **entirely offline** and are reconciled later. | M | 3 | A stocktake in flight mode, reconciled after the network returns |
| REQ-IDENT-014 | Public resolution at `/c/{code}` gives **the same** neutral response for a missing permission and for an unknown code. | M | 2 | The responses are indistinguishable |
| REQ-IDENT-015 | The base URL for code resolution is **configurable at deployment time** (`HOMEINV_PUBLIC_BASE_URL`, a required variable) and hard-coded nowhere. | M | 2 | Two instances with different bases produce different QR contents without a code change |
| REQ-IDENT-016 | Before an instance's **first** label print, the UI requires explicit confirmation of the base URL, with the note that changing it later invalidates every printed label. | M | 2 | Without confirmation no print job is accepted |
| REQ-IDENT-017 | If the configured base URL differs from one already printed onto labels, the application warns at startup **with the number of affected labels** and shows a banner in the administration UI until acknowledged. Startup is **not** blocked. | M | 2 | A test with a changed base: a warning with the correct count, the instance starts |
| REQ-IDENT-018 | `/c/{code}` is also accepted under the former bases listed in `HOMEINV_LEGACY_BASE_URLS`, so that old labels keep working after a domain change. | S | 2 | A call through a legacy hostname resolves correctly |

---

## LBL — Labels and printing

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-LBL-001 | Label templates consist of placed elements (`code`, `text`, `image`, `line`, `rect`) with data bindings. | M | 2 | A template is creatable in the UI |
| REQ-LBL-002 | The expression language in templates is **not Turing-complete** (field access, formatting, conditionals). | M | 2 | No loops, no function definitions, no external access |
| REQ-LBL-003 | Label geometries (`LabelMedia`) are maintained as **data** and cover sheet and roll stock. | M | 2 | A custom geometry is creatable without a code change |
| REQ-LBL-004 | A starter catalogue of common formats ships with the product ([`docs/reference/label-media.yaml`](../reference/label-media.yaml)). Every format carries `verified`, a source and a verification date. `verified: true` **only** when dimensions, grid, margins and pitch are all sourced — a geometry derived from size and count counts as **not** verified. | S | 2 | The catalogue exists; no entry with `verified: true` lacks a source; the UI flags unverified formats and requires the calibration sheet |
| REQ-LBL-005 | For sheet stock a **start offset** is selectable, so partly used sheets can be reused. | M | 2 | Printing from position 7 leaves the first six cells blank |
| REQ-LBL-006 | There is a **calibration sheet** with measurement marks to compensate for printer offset. | S | 2 | The offset is settable in mm and takes effect |
| REQ-LBL-007 | The preview is to scale and shows quiet zones; too small a module size raises a warning. The renderer computes the **minimum label edge** from the actual payload and the 0.33 mm floor, rather than assuming one: the shipped QR payload (`https://<base>/c/<code>#i=<uuid>`) is a version 5 symbol, 45 modules including the quiet zone, so **≈ 15 mm** — not the 10 mm [10 §10.1](../architecture/10-identification-and-labels.md) used to claim ([ADR-0030](../adr/0030-public-code-format.md)). | M | 2 | A warning below 0.33 mm module size; a template placing the shipped payload on an edge shorter than the computed minimum is rejected by the preview check, and the host-free form `homeinv:<code>` computes a smaller minimum rather than the same one |
| REQ-LBL-008 | Print jobs are persisted aggregates with state and are repeatable. | M | 2 | The state machine `QUEUED → RENDERING → RENDERED → PRINTING → COMPLETED` |
| REQ-LBL-009 | A print job records which code landed at which sheet position. | S | 2 | After a paper jam a targeted repeat is possible |
| REQ-LBL-010 | Print targets are replaceable through the `PrintTarget` port; PDF download ships with the product. | M | 2 | A printer plugin is attachable without a core change |
| REQ-LBL-011 | The source set of a print job: a single selection, a multiple selection, a **saved search**, a location subtree, or n pre-issued codes. | M | 2 | All five sources usable |
| REQ-LBL-012 | Rendered artifacts stay retrievable for 30 days. | S | 2 | Re-download without re-rendering |
| REQ-LBL-013 | Every print job records the base URL it used; the instance keeps a record of every base ever printed. A *labels with the old base* report is derivable from it. | S | 2 | The report names the affected items down to the individual one |
| REQ-LBL-014 | Machine-readable codes are **always rendered dark modules on a light background**, in both themes — on screen, in the print preview and on the printed label. The label preview keeps its paper-white ground in dark mode; only the surrounding UI follows the theme. | M | 2 | A code shown in dark mode is not inverted; the preview matches what comes out of the printer |

---

## ENR — Metadata enrichment

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-ENR-001 | Metadata is resolved from a code through the `MetadataResolver` port. | M | 3 | An ISBN resolver runs as a plugin |
| REQ-ENR-002 | Supported schemes at least: ISBN-10, ISBN-13, EAN-13, EAN-8, UPC-A, GTIN, MPN. | S | 3 | A resolver per scheme, or a documented gap |
| REQ-ENR-003 | Enrichment produces **proposals**, never direct changes. | M | 3 | A proposal with provenance, timestamp and per-field confidence |
| REQ-ENR-004 | Proposals are accepted, rejected or edited field by field. | M | 3 | Partial acceptance is possible |
| REQ-ENR-005 | Automatic adoption is switchable per tenant and per field, but is **not** the default. | S | 3 | Manual acceptance is the default |
| REQ-ENR-006 | A `FieldMapping` maps resolver output onto the fields of any target type — configurable. | M | 3 | The ISBN resolver also works for a self-defined type |
| REQ-ENR-007 | Several resolvers per scheme are possible, with priority and fallback. | S | 3 | A failure of the first resolver leads to the second |
| REQ-ENR-008 | Results are cached to spare the external services. | S | 3 | A second query for the same code causes no external call |
| REQ-ENR-009 | Per resolver it is visible and switchable which data goes to which host. | M | 3 | The target list is inspectable in the UI |

---

## SRCH — Search

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-SRCH-001 | Full-text search across **name and description** — the two fields the `item` row carries in stage 0, indexed by its **two** generated columns `search_vector_de` (`german`) and `search_vector_en` (`english`), queried by the principal's display language and `OR`-combined with the other ([ADR-0047](../adr/0047-bilingual-search-vectors.md), [07 §7.3](../architecture/07-data-model.md)). | M | 0 | Hits in both fields, with PostgreSQL alone and no OpenSearch — **and stemmed**: searching *Bohrmaschinen* finds an item named *Bohrmaschine*, and *drills* finds *drill*. The column used to be a single `simple` vector, which does no stemming at all, so this requirement was satisfiable by a substring match |
| REQ-SRCH-011 | Full-text search additionally covers **notes, attribute values, tags and the location path**, each from the stage at which that field exists. Served by OpenSearch, and in the `minimal` profile and during an OpenSearch outage by the `item` row, `item_attr_index` and the location `ltree` path — with the same two language configurations as `REQ-SRCH-001` ([ADR-0047](../adr/0047-bilingual-search-vectors.md)). | M | 1 | Hits in all four, in the OpenSearch adapter **and** in the PostgreSQL fallback, stemmed in both |

| REQ-SRCH-002 | Facets over type, category, tag, location subtree, condition and `facetable` fields. | M | 1 | Facet counts agree with the results |
| REQ-SRCH-003 | Filters over any `searchable` field, including range filters. | M | 1 | `purchasePrice >= 100` returns correct results |
| REQ-SRCH-004 | Sorting over `sortable` fields, ascending and descending. | M | 1 | Sorting is index-backed |
| REQ-SRCH-005 | Search is replaceable through the `SearchIndex` port; OpenSearch and PostgreSQL ship with the product. | M | 1 | A profile switch without a code change |
| REQ-SRCH-006 | On an OpenSearch outage, search falls back to PostgreSQL and **says so** — through `meta.degraded: true` with `degradedReason: "search-fallback"` in the response envelope, **not** through the `Warning` header, which RFC 9111 §5.5 obsoleted in 2022 ([ADR-0039](../adr/0039-degraded-response-signalling.md)). | M | 1 | The response carries the two `meta` fields and status `200`; the UI shows the state |
| REQ-SRCH-007 | Hits are always re-loaded from PostgreSQL; the index supplies IDs only. | M | 1 | A tampered index entry changes no displayed content |
| REQ-SRCH-008 | **Saved searches** are nameable and usable as smart lists. | M | 1 | Usable as a source for label printing and stocktakes |
| REQ-SRCH-009 | Pagination exclusively by **cursor**, never by offset. | M | 0 | A cursor with changed filters is rejected |
| REQ-SRCH-010 | A local search (prefix and substring) is available offline. | S | 3 | Search in flight mode returns results from the local store |

> **Why this is two rows and not one.** `REQ-SRCH-001` demanded full text across name, description, notes, attribute values, tags and location path at **stage 0**, priority **M**. The only stage-0 mechanism is the generated vector on the `item` row — since [ADR-0047](../adr/0047-bilingual-search-vectors.md) two of them, `search_vector_de` and `search_vector_en` — which covers name and description; notes are a stage-1 field (`REQ-CORE-014`), attribute values need `item_attr_index` (stage 1), and there is no OpenSearch before stage 1 at all. Four of the six field groups had no mechanism at the stage that required them — and `minimal` never gets OpenSearch, so for that profile the gap was permanent. Split on 2026-09-11: stage 0 promises what PostgreSQL alone delivers, stage 1 promises the rest **in both adapters**, which is the part that keeps `minimal` honest.


---

## LIFE — Lifecycle, maintenance, lending, valuation

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-LIFE-001 | Purchase data: price, currency, date, source, invoice attachment. | M | 1 | The fields exist and are reportable |
| REQ-LIFE-002 | Warranty: expiry date or lifetime warranty, proof attachment. | M | 1 | Expiry raises a reminder |
| REQ-LIFE-003 | A **maintenance log** per item with date, kind, cost, note, attachment. | S | 1 | Entries in chronological order, not retroactively editable |
| REQ-LIFE-004 | Maintenance intervals raise reminders. | S | 1 | The interval is settable and the reminder fires |
| REQ-LIFE-005 | **Lending**: borrower (internal or free text), handed-out date, due date, return. | S | 1 | A lent item is recognisable as such and not deletable |
| REQ-LIFE-006 | Overdue returns raise reminders. | S | 1 | A reminder to the lender and the owner |
| REQ-LIFE-007 | Sale and disposal are recorded (price, date, recipient) and change the state. | S | 1 | The state change appears in the audit log |
| REQ-LIFE-008 | Reporting: total value per location, type and tag — **per currency**, never mixed. | M | 1 | The totals agree per currency with the sum of the individual values |
| REQ-LIFE-009 | **Current value** through the `ValuationProvider` port; straight-line depreciation over a type-dependent useful life ships with the product, overridable per item. | S | 1 | The current value is computable and manually correctable |
| REQ-LIFE-010 | **Stocktake mode**: the expected list for a location, matching by scan, a discrepancy report. | M | 2 | The report names missing, unexpected and moved items |
| REQ-LIFE-011 | The stocktake report proposes corrections; nothing is applied automatically. | M | 2 | Every correction is individually confirmable |
| REQ-LIFE-012 | **Moving mode**: create and label a box, scan items into it, seal it, move it as a whole. | M | 2 | The full flow as an acceptance test |
| REQ-LIFE-013 | Expiry dates (warranty, licence, best-before) are collected in one overview. | S | 1 | The overview is sorted by due date |
| REQ-LIFE-014 | **Replacement value** as a separate field per item, with an as-of date and provenance (maintained manually or determined by a plugin). It is **independent** of the current value and never derived from it. | M | 1 | Both values are separately recordable and separately reportable |
| REQ-LIFE-015 | Reports state purchase price, current value and replacement value **separately and unambiguously labelled** — per location, type and tag. The three figures are never mixed. | M | 1 | Totals agree per figure with the sum of the individual values; labelling is unambiguous |
| REQ-LIFE-016 | An **insurance report** is producible: replacement value per room and in total, with the primary photo, the purchase receipt and the as-of date per item, as a PDF and as a table. | S | 1 | The report is producible for a location subtree and is complete |
| REQ-LIFE-017 | Every monetary total is computed and displayed **per currency**. Where a set spans several currencies, the report shows one line per currency and states that no conversion took place — a mixed-currency total is never produced, not even as an approximation. | M | 1 | A tenant with EUR and USD items gets two lines and an explicit note, never one number |

---

## TEN — Tenants, memberships, roles

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-TEN-001 | All data belongs to a tenant; the separation is secured through RLS independently of the application logic. | M | 0 | The automated isolation proof across every table |
| REQ-TEN-002 | An entitled user can create **any number of tenants**, bounded by a quota. | M | 1 | Creation and quota enforcement verified |
| REQ-TEN-003 | Users are members of several tenants and switch without re-authenticating. | M | 1 | Switching in the UI and through the API |
| REQ-TEN-004 | Invitations are single-use, time-limited and bound to an e-mail address. | M | 1 | Reuse is rejected |
| REQ-TEN-005 | Built-in roles: `OWNER`, `ADMIN`, `MEMBER`, `CONTRIBUTOR`, `VIEWER`, `GUEST`. | M | 1 | The permission matrix is documented and tested |
| REQ-TEN-006 | Tenant-owned roles extend the built-in ones; permissions are individually settable. | S | 1 | A custom role is creatable and assignable |
| REQ-TEN-007 | A role can be scoped to a **location subtree**. | S | 1 | A user scoped to "garage" sees nothing beyond it |
| REQ-TEN-008 | Field visibility is controllable per role; `sensitive` fields are **removed**, not masked. | M | 1 | The response does not contain the field at all |
| REQ-TEN-009 | Quotas per tenant: item count, bytes of storage, plugin count, API calls. | M | 1 | Exceeding one returns `403` with the current and permitted amount |
| REQ-TEN-010 | Nobody can grant permissions they do not hold themselves. | M | 1 | The attempt is rejected and logged |
| REQ-TEN-011 | Tenant deletion with a 30-day grace period, revocation, and **a completion report per building block**. | M | 1 | An erasure certificate is produced |
| REQ-TEN-012 | **A fresh instance gets its first owner and tenant from a one-shot service**, not from a registration endpoint and not from SQL: the operator sets `HOMEINV_BOOTSTRAP_EMAIL` and mounts `HOMEINV_BOOTSTRAP_PASSWORD_FILE`, and the `bootstrap` role creates the account, the tenant and its catalogue and exits ([ADR-0053](../adr/0053-first-owner-as-a-one-shot.md)). It is idempotent: a second run finds the address taken, changes nothing and exits 0, so a redeploy neither creates a second owner nor resets a password the operator has changed. Without it nothing could create either — `TenantProvisioningService` was reachable only from tests — so nobody could sign in to a deployed instance and stage 0's own definition of done was unreachable through the product. | M | 0 | A stack brought up by `deploy/setup.sh` can be signed in to; running the service a second time creates nothing and exits 0; with neither variable set it exits non-zero and `api` does not start |

---

## AUTH — Login and sessions

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-AUTH-001 | Login with e-mail and password (Argon2id). | M | 0 | Hash parameters configurable and upgraded on login |
| REQ-AUTH-002 | Second factor through TOTP and WebAuthn/passkeys. | M | 1 | Both usable; recovery codes single-use |
| REQ-AUTH-003 | The second factor is **mandatory** for `OWNER` and `ADMIN`, **and for any role that may read `sensitive` fields** (`REQ-SEC-015`). Otherwise `REQ-AUTH-011` would demand a second factor from an account that was never required to own one. The role is **granted and locked**: the membership is written, and the role cannot be *used* until the factor exists ([ADR-0061](../adr/0061-second-factor-locks-the-role.md)) — refusing the assignment would leave a tenant nobody owns, because creating one makes an owner and the first owner of an instance is made by a one-shot with nobody to ask for a code. | M | 1 | A session holding such a role with no second factor is refused **every** request in that tenant with `second-factor-missing`, the first owner included; `/api/v1/auth/**` and `/api/v1/me/**` keep answering, so enrolling and switching tenants stay reachable; granting `sensitive` field visibility to a role whose members lack one is rejected, with a count and no names |
| REQ-AUTH-004 | Registration by invitation only by default (`invite_only`). | M | 1 | Open registration is an explicit operator setting |
| REQ-AUTH-005 | OIDC federation through the `IdentityProvider` port (Authorization Code + PKCE), served by `plugin-oidc` ([ADR-0026](../adr/0026-core-outbound-via-plugins.md)). `state` and the PKCE verifier are held **server-side**, never in a cookie ([ADR-0029](../adr/0029-session-cookie-and-oidc-state.md)). | S | 1 | Login through an external provider works; a replayed callback fails because the handle is single-use |
| REQ-AUTH-006 | **No automatic account linking** by e-mail address alone. | M | 1 | Linking requires re-authentication |
| REQ-AUTH-007 | Apps and third-party systems use OAuth 2.1 with PKCE through the system browser; **no password in the app**. | M | 3 | The flow is verified |
| REQ-AUTH-008 | Access tokens valid 10 min; refresh tokens rotating with reuse detection. | M | 1 | Reuse terminates all of the user's sessions |
| REQ-AUTH-009 | The user sees their active sessions and devices and can terminate them individually. | M | 1 | Remote sign-out takes effect within 10 min |
| REQ-AUTH-010 | Service accounts with limited permissions, tenant binding and an expiry date. | S | 1 | The token is shown in clear exactly once |
| REQ-AUTH-011 | Critical operations require **re-confirmation** of the second factor. The proof counts for **fifteen minutes**, recorded in the session; a code goes to `POST /api/v1/auth/mfa/step-up`. An operation past the window is refused with `second-factor-stale`; a `sensitive` field is **removed** instead and the answer stays `200`, because a list with one sensitive column in it would otherwise become unreadable. | M | 1 | Granting permissions, exporting, deleting a tenant, viewing `sensitive` fields — each refused or redacted once the window has passed, and permitted again after a code, which `SecondFactorStepUpIT` drives end to end |

---

## API — Interfaces

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-API-001 | A REST API under `/api/v1`, fully described in OpenAPI 3.1. **The document is generated from the implementation and committed**; hand-editing it fails the drift check ([ADR-0049](../adr/0049-openapi-generated-from-the-implementation.md)). It used to read *“the specification is the source”*, with server stubs and clients generated from it — a promise stage 0 did not keep, and one whose value was the agreement between document and implementation rather than the direction it was produced in. The **plugin** contract in [`proto/`](../../proto/) stays specification-first, because it is agreed with parties who do not implement it. | M | 0 | `./gradlew openApiCheck` regenerates the document and fails on any difference; `spectral` lints it against the project ruleset; `oasdiff` reports a breaking change against the last release; every example in it validates against its schema |
| REQ-API-002 | Generated clients for TypeScript (web) and Kotlin (KMP) compile in CI. | M | 1 | Both builds green |
| REQ-API-003 | Errors follow RFC 9457 with a stable `type` URI, a `traceId` and field paths. The set of `type` values is the registry at [`docs/reference/problem-types.yaml`](../reference/problem-types.yaml); the URI is fixed for the product, **not** derived from the deployment's hostname, or clients could not branch on it across instances. | M | 0 | Uniform across every endpoint; every `type` a response emits is in the registry and every registry entry with a `since` at or below the current stage is reachable by a test; `oasdiff` sees a removed token or a changed status code as breaking |
| REQ-API-004 | `ETag`/`If-Match` on single resources; a missing header → `428`, a conflict → `412`. | M | 1 | Blind overwriting is impossible |
| REQ-API-005 | `Idempotency-Key` on every creating `POST`, valid for 24 h. The record is written **in the same transaction as the entity it protects**, in PostgreSQL — never in a cache ([ADR-0009](../adr/0009-messaging-and-events.md)). | M | 1 | A repeat returns the original result; a repeat **after the cache has been cleared** still returns the original result rather than creating a second record |
| REQ-API-006 | A read-only **GraphQL** surface with depth and cost limits and persisted queries. | S | 1 | No mutations exist |
| REQ-API-007 | A **gRPC** contract `home_inv.plugin.v1` for plugins, with `buf lint` and `buf breaking` in CI. | M | 1 | A break fails the build |
| REQ-API-008 | The major version in the path; breaks only with a new major version and at least 12 months of parallel operation. | M | 1 | `oasdiff` in CI; `Deprecation`/`Sunset` headers |
| REQ-API-009 | Usage per endpoint, version and client is measured. | M | 1 | The metric exists; it is the basis for shutdown decisions |
| REQ-API-010 | **Webhooks** with a signature, retries and a delivery log, delivered by `plugin-webhook` — never by the core, which fetches no user-supplied URL (`REQ-SEC-034`). The signature scheme is part of the plugin contract: HMAC-SHA256 over timestamp and body, in a named header, with the timestamp inside the signed material so a capture cannot be replayed. | S | 1 | The receiver can verify the signature; a replayed delivery outside the tolerance window is rejected; a network test shows the core reaching no webhook target |
| REQ-API-011 | Live updating of open views through SSE. | S | 1 | A change by another user appears without a reload |
| REQ-API-012 | **Degradation is signalled in the response envelope**, through `meta.degraded` and a stable `meta.degradedReason` token — never through the `Warning` header (obsoleted by RFC 9111 §5.5) and never through the status code: a degraded response is `200`, because its content is correct and only its facets and ranking are poorer ([ADR-0039](../adr/0039-degraded-response-signalling.md)). Every degradation level of [13 §13.6](../architecture/13-operations-and-observability.md) that a client can observe signals this way. | M | 1 | `meta.degraded` is part of the OpenAPI schema, so omitting it is a contract break; every token a response emits is in [`docs/reference/degraded-reasons.yaml`](../reference/degraded-reasons.yaml), and every client-observable level of [13 §13.6](../architecture/13-operations-and-observability.md) has an entry there or an entry in the registry's `excluded` list saying why it cannot be one; a new `degradedReason` token passes `oasdiff` as a minor change; no response carries a `Warning` header |

---

## PLG — Plugins and extensibility

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-PLG-001 | Extension points as ports: `CodeFormat`, `ScanSource`, `LabelRenderer`, `PrintTarget`, `LabelMediaProvider`, `MetadataResolver`, `BlobStore`, `SearchIndex`, `NotificationChannel`, `IdentityProvider`, `ImageProcessor`, `VirusScanner`, `ValuationProvider`, `ImportMapper`. | M | 1 | The fourteen **ports exist** and the plugin runtime resolves them at stage 1 ([ADR-0028](../adr/0028-plugin-runtime-stage-1.md), which moved this row forward for the *runtime*). Each port has at least one shipped implementation **by the stage at which that port's own feature ships** — `ImageProcessor` and `VirusScanner` at stage 0, `BlobStore`, `SearchIndex`, `NotificationChannel`, `IdentityProvider`, `ValuationProvider` and `ImportMapper` at stage 1, `CodeFormat`, `ScanSource`, `LabelRenderer`, `PrintTarget` and `LabelMediaProvider` at stage 2, `MetadataResolver` at stage 3. The criterion read "at least one shipped implementation per port" without qualification until 2026-09-11 (**O21**), which made stage 1 unsignable-off against six ports whose features ship later |
| REQ-PLG-002 | Out-of-process plugins over gRPC with mTLS are the **default** for third-party code. | M | 1 | An example plugin in Java and in Python runs |
| REQ-PLG-003 | In-process plugins are **off** by default and permitted only for signed artifacts explicitly enabled by the operator. | M | 1 | A tenant administrator cannot install them |
| REQ-PLG-004 | Every plugin brings a signed **manifest** with its contract range, ports, capabilities and settings. | M | 1 | An unsigned plugin only with an explicit operator setting |
| REQ-PLG-005 | Capabilities are granted **per tenant**; without a grant nothing is possible. | M | 1 | A call without the capability is rejected and logged |
| REQ-PLG-006 | A manifest change adding a capability resets the consent. | M | 1 | No silent privilege escalation |
| REQ-PLG-007 | Every plugin call has a deadline, a payload limit, a bulkhead and a circuit breaker. | M | 1 | A slow plugin does not block the core |
| REQ-PLG-008 | A plugin with an incompatible contract version is disabled and reported; the core starts normally. | M | 1 | Foreign code can never prevent startup |
| REQ-PLG-009 | An SDK is provided in **five languages, all of them first-class**: **Java, Kotlin, Rust, Python and Go** — each with server scaffolding, manifest validation, a health endpoint, logging and test helpers — plus the protobuf module, the **contract test suite** and a project template. Kotlin is its own artefact and not "the Java one is usable from Kotlin": an idiomatic API with coroutines rather than futures. *Widened from Java and Python on 2026-09-12 (@greluc).* | M | 3 | `homeinv-plugin init --lang <java\|kotlin\|rust\|python\|go>` produces a runnable scaffold in each, and the contract test suite passes against a plugin written in each |
| REQ-PLG-010 | A new code format plugin runs **without a single line of core change**. | M | 1 | An automated test in CI following [09 §9.10](../architecture/09-extensibility-and-plugins.md) |
| REQ-PLG-011 | Plugin writes appear in the audit log **with the plugin as the actor**. | M | 1 | Distinguishable from user actions |
| REQ-PLG-012 | Plugin UI panels (`ui:panel`) run in a sandboxed `iframe` with their own origin. The panel serves its own CSP with `frame-ancestors` restricted to the application origin, and needs **no** `Cross-Origin-Embedder-Policy` — the application does not set COEP ([ADR-0040](../adr/0040-no-cross-origin-isolation.md)). | S | 3 | Communication only through `postMessage` with a verified origin and a fixed message schema; a panel loads from a plugin built against the published SDK alone |
| REQ-PLG-013 | There is **no** installation from inside the running system out of the internet. | M | 1 | Installation stays an operator action |
| REQ-PLG-014 | A curated list of known plugins exists as `PLUGINS.md` in the repository (name, publisher, ports, manifest URL, certificate fingerprint, contract version, licence). Admission by pull request after passing the contract test suite. The head of the list makes clear that the project **neither distributes nor counter-signs nor assures security**. | S | 3 | The list exists; the admission criteria are documented |
| REQ-PLG-015 | An outbound plugin **verifies its target completely before it serves, and refuses service on a partial state** rather than working partially: the folder exists *and* is writable, the bucket exists *and* the agreed prefix is reachable, the mailbox authenticates *and* accepts the envelope sender. A half-provisioned target reports `PROVISIONING_INCOMPLETE` ([`docs/reference/plugin-health-states.yaml`](../reference/plugin-health-states.yaml)) and accepts nothing. | M | 1 | Against a target whose folder exists but is not writable, the plugin reports the state and rejects every write; no write is accepted that cannot later be read back |

---

## SYNC — Offline reconciliation

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-SYNC-001 | Web and apps keep the visible inventory locally and work without a network. | M | 3 | Full use in flight mode |
| REQ-SYNC-002 | Aggregates can be created, edited, moved and lent offline. | M | 3 | All operations possible offline |
| REQ-SYNC-003 | Reconciliation goes through a server-side change log with a monotonic sequence per tenant. | M | 3 | Ordering is deterministic |
| REQ-SYNC-004 | Conflicts are detected through a **three-way compare** (base, local, server). | M | 3 | Disjoint field changes merge automatically |
| REQ-SYNC-005 | Conflicts that cannot be resolved automatically produce a **conflict record** with both versions. | M | 3 | No change is lost |
| REQ-SYNC-006 | The resolution rules per field kind are documented and implemented identically in web and apps. | M | 3 | A shared test suite runs against both |
| REQ-SYNC-007 | `sensitive` fields are **always** a conflict, never an automatic merge. | M | 3 | A test exists |
| REQ-SYNC-008 | Push is idempotent through `changeId`; a network drop after the write is harmless. | M | 3 | A repeat returns `DUPLICATE` |
| REQ-SYNC-009 | A device with an expired cursor is asked to perform a full seeding (`410`). | M | 3 | The flow is verified |
| REQ-SYNC-010 | Withdrawing a permission removes the affected data from the device at the next reconciliation. | M | 3 | Proof that nothing remains locally |
| REQ-SYNC-011 | The user sees their devices with the last reconciliation and any unsent changes, and can wipe them remotely. | M | 3 | A remote wipe first transmits unsent changes where possible |
| REQ-SYNC-012 | Configuration and permissions are **download only**, never edited offline. | M | 3 | Offline editing is not offered in the UI at all |
| REQ-SYNC-013 | Property-based tests with n simulated devices show that no record disappears without a conflict record. | M | 3 | The test run is part of CI |
| REQ-SYNC-014 | `/sync/pull` is filtered **per principal**, not merely per tenant: every entry passes the same authorization check as a REST read of that entity, including location-subtree scope. | M | 3 | A user scoped to one subtree receives no entry from outside it — verified against the raw `change_log`, not against the API response |
| REQ-SYNC-015 | `sensitive` field values never appear in a sync payload or tombstone, by the same rule as REST and GraphQL. | M | 3 | A shared test across REST, GraphQL and sync asserts the field is **absent**, not masked |
| REQ-SYNC-016 | A change to a numeric field carries its **intent** (`SET` or `ADJUST`); the client derives it from the interaction, never from a dialog. Two `ADJUST`s merge by summing deltas, two `SET`s conflict, a `SET` against an `ADJUST` takes the `SET` as base ([ADR-0014](../adr/0014-offline-synchronisation.md)). | M | 3 | A stocktake correction concurrent with an offline withdrawal yields the counted value minus the withdrawal — **not** the withdrawal applied twice |

---

## NOTI — Notifications and reminders

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-NOTI-001 | Reminder rules consist of a saved search, a time offset and a channel — as **data**, not code. | M | 1 | A rule is creatable in the UI |
| REQ-NOTI-002 | Channels through the `NotificationChannel` port. **Every** channel is a plugin — including e-mail and webhook — because every one of them leaves the deployment ([ADR-0026](../adr/0026-core-outbound-via-plugins.md)). `plugin-smtp` and `plugin-webhook` ship as first-party plugins. | M | 1 | Both run as plugins; a network test shows the core reaching neither an SMTP server nor a webhook target |
| REQ-NOTI-003 | Triggers at least: warranty expiry, maintenance interval, return date, minimum stock, licence expiry, stocktake discrepancy. | S | 1 | An acceptance test per trigger |
| REQ-NOTI-004 | Security-relevant account events are always reported by e-mail and cannot be switched off **by a user or a tenant administrator**. They do depend on `plugin-smtp` being installed by the operator; an installation without it has no mail at all, and the administration UI states that rather than dropping notifications silently ([ADR-0028](../adr/0028-plugin-runtime-stage-1.md)). | M | 1 | Password change, a new second factor, a new device, remote sign-out — each delivered; with the plugin absent, the UI shows the reduced state |
| REQ-NOTI-005 | Delivery attempts are logged, with retries and dead-lettering. | S | 1 | A failed delivery is visible |
| REQ-NOTI-006 | Users configure per channel and per kind what they receive. | S | 1 | The settings take effect |
| REQ-NOTI-007 | Push goes through `NotificationChannel` **plugins** — Firebase, APNs **and Web Push/VAPID**, which reaches the browser vendors' push endpoints and is therefore no more "core-safe" than the other two ([ADR-0023](../adr/0023-push-notifications.md), [ADR-0026](../adr/0026-core-outbound-via-plugins.md)). | M | 3 | A network test: **`api` and `worker`** reach no external host — *not* "no core container", which `web` and `egress-proxy` contradict by design ([ADR-0042](../adr/0042-edge-is-not-internal.md), `REQ-SEC-102`); each push plugin reaches only the hosts in its manifest, enforced by the egress proxy |
| REQ-NOTI-008 | The push payload contains **no inventory data** — only an identifier and a category (`reminder`, `security`, `conflict`). The app fetches the content through the authenticated API. | M | 3 | A captured push message contains neither item name nor location, amount or tenant name |
| REQ-NOTI-009 | Push requires three separate opt-ins: the operator installs the plugin, the tenant administrator grants `network:outbound`, the user switches it on. Everything keeps working without push (e-mail and fetch). | M | 3 | Each level verified individually; switching off at any level takes effect |

---

## PORT — Import, export, data subject access

| ID | Requirement | Prio | Stage | Acceptance |
|---|---|---|---|---|
| REQ-PORT-001 | CSV import with a configurable mapping profile, a preview and a **dry run**. | M | 1 | The dry run shows errors without writing |
| REQ-PORT-002 | Mapping profiles for **Homebox** and **InvenTree** ship with the product. | S | 1 | A sample export of either system is importable |
| REQ-PORT-003 | A full tenant export as a ZIP with data, configuration, media and a manifest. | M | 1 | The export is importable into another instance |
| REQ-PORT-004 | The export contains the type definitions, so that a tenant can move entirely. | M | 1 | The move as an acceptance test |
| REQ-PORT-005 | Export runs asynchronously as a job resource with progress. | M | 1 | `202` plus a job resource |
| REQ-PORT-006 | GDPR access (Art. 15) and portability (Art. 20) are producible mechanically. | M | 1 | The archive contains all of the user's personal data |
| REQ-PORT-007 | An import creates no partial state: either complete or not at all, with an error report. | M | 1 | An abort leaves no records behind |
| REQ-PORT-008 | Imported items keep their provenance (source, timestamp, original key). | S | 1 | The provenance is visible in the history |
