/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package de.greluc.homeinv.client.models


import kotlinx.serialization.*
import kotlinx.serialization.descriptors.*
import kotlinx.serialization.encoding.*

/**
 * The declarative limits a field puts on its values — the whole of them (ADR-0020).  Every member is expressible in JSON Schema and evaluable with the value in hand, which is what lets a client offline reach the same verdict as the server (REQ-CORE-027). There is deliberately nothing here that reads another item, calls out, or evaluates an expression: those are the three things ADR-0020 excludes, and the place they belong is a plugin.  A `null` member is no constraint at all. A record rather than a map, so that a typo in a constraint name is a refused field definition rather than a limit that silently does nothing.
 *
 * @param empty Whether this declares no limit whatsoever.
 * @param max the largest permitted number, by the same rule
 * @param maxLength the most characters. Absent, the 64 KiB ceiling on the whole attribute set is the only limit, which is a limit on the wrong thing
 * @param min the smallest permitted number, for `integer`, `decimal`, `money` and `quantity` — compared against the amount, never against the unit
 * @param minLength the fewest characters, for the text-shaped kinds
 * @param pattern a regular expression the whole value must match, for `text`, ` multiline`, `url`, `email` and `secret`. Anchored by the validator, so `[0-9]+` means the entire value and not a digit somewhere in it
 * @param referenceKind what a `reference` field may point at
 * @param unit the one unit a `quantity` field accepts, or the one ISO 4217 code a ` money` field accepts. Declaring it is what makes a total over the field meaningful: REQ-CORE-032 forbids a mixed-unit sum, and a field that accepts any unit can only be summed per unit
 */
@Serializable

data class FieldConstraints (

    /* Whether this declares no limit whatsoever. */
    @SerialName(value = "empty") @Required val empty: kotlin.Boolean,

    /* the largest permitted number, by the same rule */
    @SerialName(value = "max") @Required val max: kotlin.Double,

    /* the most characters. Absent, the 64 KiB ceiling on the whole attribute set is the only limit, which is a limit on the wrong thing */
    @SerialName(value = "maxLength") @Required val maxLength: kotlin.Int,

    /* the smallest permitted number, for `integer`, `decimal`, `money` and `quantity` — compared against the amount, never against the unit */
    @SerialName(value = "min") @Required val min: kotlin.Double,

    /* the fewest characters, for the text-shaped kinds */
    @SerialName(value = "minLength") @Required val minLength: kotlin.Int,

    /* a regular expression the whole value must match, for `text`, ` multiline`, `url`, `email` and `secret`. Anchored by the validator, so `[0-9]+` means the entire value and not a digit somewhere in it */
    @SerialName(value = "pattern") @Required val pattern: kotlin.String,

    /* what a `reference` field may point at */
    @SerialName(value = "referenceKind") @Required val referenceKind: FieldConstraints.ReferenceKind,

    /* the one unit a `quantity` field accepts, or the one ISO 4217 code a ` money` field accepts. Declaring it is what makes a total over the field meaningful: REQ-CORE-032 forbids a mixed-unit sum, and a field that accepts any unit can only be summed per unit */
    @SerialName(value = "unit") @Required val unit: kotlin.String

) {

    /**
     * what a `reference` field may point at
     *
     * Values: ITEM,LOCATION,VALUE_LIST
     */
    @Serializable
    enum class ReferenceKind(val value: kotlin.String) {
        @SerialName(value = "ITEM") ITEM("ITEM"),
        @SerialName(value = "LOCATION") LOCATION("LOCATION"),
        @SerialName(value = "VALUE_LIST") VALUE_LIST("VALUE_LIST");
        override fun toString(): kotlin.String = value
    }

}

