/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package de.greluc.homeinv.client.models

import de.greluc.homeinv.client.models.ValuationRequest

import kotlinx.serialization.*
import kotlinx.serialization.descriptors.*
import kotlinx.serialization.encoding.*

/**
 * The body of a creation.  The constraints are duplicated from the aggregate on purpose. The aggregate's checks are the truth and hold for every caller; these produce a `422` with a field path, which is what lets a form mark the field instead of showing a sentence. A rule that existed only here would be one a non-HTTP caller could walk past.
 *
 * @param kind `PHYSICAL` or `DIGITAL`
 * @param name the name
 * @param attributes the fields the type declares, as a JSON object. Checked against the version's schema, and an offending value is a `422` naming its path (REQ-CORE-005)
 * @param description free text
 * @param id the client's chosen UUIDv7, or `null` to have one assigned
 * @param itemTypeId the type, or omitted — the server then uses the tenant's built-in type, which is what a quick capture does and what every stage-0 client did. The TYPE and not one of its versions: a person picks \"book\", and which version that is today is the server's to know (REQ-CORE-025)
 * @param locationId required for a physical item
 * @param maintenanceIntervalDays how often it needs servicing, in days, or omitted when nothing should remind about it (REQ-LIFE-004). Days and not months, because \"every 3 months from 31 January\" has no answer that is not a surprise to somebody
 * @param minimumStock the level below which this consumable needs restocking, or omitted
 * @param notes a paragraph in limited Markdown; HTML is removed before it is stored
 * @param quantity how many; `null` means one
 * @param quantityUnit the unit
 * @param valuation what it cost, what covers it and what replacing it would cost, or omitted
 */
@Serializable

data class CreateItemRequest (

    /* `PHYSICAL` or `DIGITAL` */
    @SerialName(value = "kind") @Required val kind: CreateItemRequest.Kind,

    /* the name */
    @SerialName(value = "name") @Required val name: kotlin.String,

    /* the fields the type declares, as a JSON object. Checked against the version's schema, and an offending value is a `422` naming its path (REQ-CORE-005) */
    @SerialName(value = "attributes") val attributes: kotlin.String? = null,

    /* free text */
    @SerialName(value = "description") val description: kotlin.String? = null,

    /* the client's chosen UUIDv7, or `null` to have one assigned */
    @SerialName(value = "id") val id: kotlin.String? = null,

    /* the type, or omitted — the server then uses the tenant's built-in type, which is what a quick capture does and what every stage-0 client did. The TYPE and not one of its versions: a person picks \"book\", and which version that is today is the server's to know (REQ-CORE-025) */
    @SerialName(value = "itemTypeId") val itemTypeId: kotlin.String? = null,

    /* required for a physical item */
    @SerialName(value = "locationId") val locationId: kotlin.String? = null,

    /* how often it needs servicing, in days, or omitted when nothing should remind about it (REQ-LIFE-004). Days and not months, because \"every 3 months from 31 January\" has no answer that is not a surprise to somebody */
    @SerialName(value = "maintenanceIntervalDays") val maintenanceIntervalDays: kotlin.Int? = null,

    /* the level below which this consumable needs restocking, or omitted */
    @SerialName(value = "minimumStock") val minimumStock: kotlin.Double? = null,

    /* a paragraph in limited Markdown; HTML is removed before it is stored */
    @SerialName(value = "notes") val notes: kotlin.String? = null,

    /* how many; `null` means one */
    @SerialName(value = "quantity") val quantity: kotlin.Double? = null,

    /* the unit */
    @SerialName(value = "quantityUnit") val quantityUnit: kotlin.String? = null,

    /* what it cost, what covers it and what replacing it would cost, or omitted */
    @SerialName(value = "valuation") val valuation: ValuationRequest? = null

) {

    /**
     * `PHYSICAL` or `DIGITAL`
     *
     * Values: PHYSICAL,DIGITAL
     */
    @Serializable
    enum class Kind(val value: kotlin.String) {
        @SerialName(value = "PHYSICAL") PHYSICAL("PHYSICAL"),
        @SerialName(value = "DIGITAL") DIGITAL("DIGITAL");
        override fun toString(): kotlin.String = value
    }

}

