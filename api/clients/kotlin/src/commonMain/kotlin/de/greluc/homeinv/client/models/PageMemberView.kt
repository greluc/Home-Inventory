/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package de.greluc.homeinv.client.models

import de.greluc.homeinv.client.models.Facet
import de.greluc.homeinv.client.models.MemberView
import de.greluc.homeinv.client.models.Meta
import de.greluc.homeinv.client.models.PageInfo

import kotlinx.serialization.*
import kotlinx.serialization.descriptors.*
import kotlinx.serialization.encoding.*

/**
 * One page of anything, in the shape 08 §8.2 specifies for every collection.  `{\"data\": […], \"page\": {…`, \"meta\": {…}}}. Twenty listings each had a record of their own carrying `items` and `nextCursor`, which was the same shape written twenty times and none of them could say that a derived store had gone down.  ## Why one type rather than twenty  The envelope is a property of the API and not of any one block, so it belongs where every block can reach it. The alternative — adding `meta` to the one listing that can degrade — was considered and rejected with the owner on 2026-09-14: the document has described one shape since the first commit, and two shapes in one API is the drift that document exists to prevent.  ## The parts that are filled in later  A block knows its rows and its cursor. It does not know how long the request took, and only the search path knows whether it answered from the fallback. Both are therefore optional and are set by whoever does know — each through a method returning a new page, because this one is a record and stays one.  ## Facets are a fourth member, and usually absent  Counts are neither rows, nor where the next rows are, nor how the answer was produced, so they are none of the three members that exist — `meta` says how a response came about and would stop meaning that if it also carried content. A fourth member is additive, and it stays `null` on every listing that was not asked for counts. Decided with the owner on 2026-09-14.  `facets` is the **one** member this API omits when it is null, and it is annotated for it rather than relying on a global setting — the rest of the API writes its nulls out. The reason is that here the difference carries meaning: absent is \"nobody asked\", `[]` is \"counted, and nothing matched\", and a client that cannot tell those apart shows an empty sidebar where there should be none. Twenty listings that can never carry counts also have no business carrying an empty field for them. Decided with the owner on 2026-09-14; 08 §8.2 says the same.
 *
 * @param `data` the rows
 * @param facets how many rows each value of a counted dimension holds, or `null` when none were asked for — which is every listing but a search that carried `facet`. Omitted from the JSON when null, which is what tells \"nobody asked\" from \"nothing matched\"
 * @param meta what was true of the answer itself rather than of the rows
 * @param page where the next ones are, and roughly how many there are in total
 */
@Serializable

data class PageMemberView (

    /* the rows */
    @SerialName(value = "data") @Required val `data`: kotlin.collections.List<MemberView>,

    /* how many rows each value of a counted dimension holds, or `null` when none were asked for — which is every listing but a search that carried `facet`. Omitted from the JSON when null, which is what tells \"nobody asked\" from \"nothing matched\" */
    @SerialName(value = "facets") @Required val facets: kotlin.collections.List<Facet>,

    /* what was true of the answer itself rather than of the rows */
    @SerialName(value = "meta") @Required val meta: Meta,

    /* where the next ones are, and roughly how many there are in total */
    @SerialName(value = "page") @Required val page: PageInfo

) {


}

