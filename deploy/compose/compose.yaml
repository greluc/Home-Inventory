# GENERATED FROM ../services.yaml — DO NOT EDIT.
#
# Run `python deploy/generate.py` after changing the matrix. CI regenerates
# this file and fails on any difference, so an edit here is lost and noticed
# rather than lost and not (REQ-NFR-063).


name: home-inv

services:
  web:
    image: ghcr.io/greluc/home-inv-web:latest
    container_name: homeinv-web
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      - edge
      - frontend
    tmpfs:
      - /tmp
      - /var/cache/nginx
      - /var/run
    ports:
      - "8080:8080"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/healthcheck"]
      interval: 15s
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 64m

  api:
    image: ghcr.io/greluc/home-inv:latest
    container_name: homeinv-api
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      - internal
      - frontend
    environment:
      SPRING_PROFILES_ACTIVE: "api,${HOMEINV_PROFILE}"
      HOMEINV_DB_URL: "jdbc:postgresql://postgres:5432/homeinv"
      HOMEINV_DB_USER: "homeinv_app"
      MANAGEMENT_SERVER_PORT: "8090"
      MANAGEMENT_SERVER_ADDRESS: "${INTERNAL_ADDR}"
      HOMEINV_TRUSTED_PROXIES: "${HOMEINV_TRUSTED_PROXIES}"
      HOMEINV_PUBLIC_BASE_URL: "${HOMEINV_PUBLIC_BASE_URL}"
      HOMEINV_MEDIA_BASE_URL: "${HOMEINV_MEDIA_BASE_URL}"
      HOMEINV_BLOBSTORE_ENDPOINT: "blobstore:8100"
      HOMEINV_BLOBSTORE_FINGERPRINT: "${HOMEINV_BLOBSTORE_FINGERPRINT}"
      HOMEINV_VALKEY_USER: "homeinv"
      HOMEINV_MQ_USER: "homeinv"
      HOMEINV_SEARCH_USER: "homeinv"
      HOMEINV_DB_PASSWORD_FILE: "/run/secrets/db-password"
      HOMEINV_DATA_ENCRYPTION_MASTER_KEY_FILE: "/run/secrets/data-encryption-master-key"
      HOMEINV_JWT_SIGNING_KEY_FILE: "/run/secrets/jwt-signing-key"
      HOMEINV_URL_SIGNING_KEY_FILE: "/run/secrets/url-signing-key"
      HOMEINV_VALKEY_PASSWORD_FILE: "/run/secrets/valkey-password"
      HOMEINV_MQ_PASSWORD_FILE: "/run/secrets/mq-password"
      HOMEINV_SEARCH_PASSWORD_FILE: "/run/secrets/search-password"
      HOMEINV_MTLS_CORE_FILE: "/run/secrets/mtls-core"
    secrets:
      - db-password
      - data-encryption-master-key
      - jwt-signing-key
      - url-signing-key
      - valkey-password
      - mq-password
      - search-password
      - mtls-core
    tmpfs:
      - /tmp
    depends_on:
      postgres:
        condition: service_healthy
      valkey:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      bootstrap:
        condition: service_completed_successfully
      blobstore:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/usr/bin/healthcheck"]
      interval: 15s
      start_period: 90s
    deploy:
      resources:
        limits:
          memory: 1536m
        reservations:
          memory: 768m

  worker:
    image: ghcr.io/greluc/home-inv:latest
    container_name: homeinv-worker
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      - internal
      - scanner
    environment:
      SPRING_PROFILES_ACTIVE: "worker,${HOMEINV_PROFILE}"
      HOMEINV_DB_URL: "jdbc:postgresql://postgres:5432/homeinv"
      HOMEINV_DB_USER: "homeinv_app"
      MANAGEMENT_SERVER_PORT: "8090"
      MANAGEMENT_SERVER_ADDRESS: "${INTERNAL_ADDR}"
      HOMEINV_DB_MIGRATE_ON_START: "false"
      HOMEINV_PUBLIC_BASE_URL: "${HOMEINV_PUBLIC_BASE_URL}"
      HOMEINV_MEDIA_BASE_URL: "${HOMEINV_MEDIA_BASE_URL}"
      HOMEINV_BLOBSTORE_ENDPOINT: "blobstore:8100"
      HOMEINV_BLOBSTORE_FINGERPRINT: "${HOMEINV_BLOBSTORE_FINGERPRINT}"
      HOMEINV_VALKEY_USER: "homeinv"
      HOMEINV_MQ_USER: "homeinv"
      HOMEINV_SEARCH_USER: "homeinv"
      HOMEINV_DB_PASSWORD_FILE: "/run/secrets/db-password"
      HOMEINV_DATA_ENCRYPTION_MASTER_KEY_FILE: "/run/secrets/data-encryption-master-key"
      HOMEINV_URL_SIGNING_KEY_FILE: "/run/secrets/url-signing-key"
      HOMEINV_VALKEY_PASSWORD_FILE: "/run/secrets/valkey-password"
      HOMEINV_MQ_PASSWORD_FILE: "/run/secrets/mq-password"
      HOMEINV_SEARCH_PASSWORD_FILE: "/run/secrets/search-password"
      HOMEINV_MTLS_CORE_FILE: "/run/secrets/mtls-core"
    secrets:
      - db-password
      - data-encryption-master-key
      - url-signing-key
      - valkey-password
      - mq-password
      - search-password
      - mtls-core
    tmpfs:
      - /tmp
    depends_on:
      postgres:
        condition: service_healthy
      clamav:
        condition: service_healthy
      blobstore:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/usr/bin/healthcheck"]
      interval: 15s
      start_period: 90s
    deploy:
      resources:
        limits:
          memory: 1536m
        reservations:
          memory: 512m

  migrate:
    image: ghcr.io/greluc/home-inv:latest
    container_name: homeinv-migrate
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: "no"
    profiles:
      - minimal
      - standard
      - ha
    networks:
      - internal
    environment:
      SPRING_PROFILES_ACTIVE: "migrate"
      HOMEINV_DB_MIGRATE_ON_START: "true"
      HOMEINV_DB_URL: "jdbc:postgresql://postgres:5432/homeinv"
      HOMEINV_DB_MIGRATION_USER: "homeinv_migrator"
      HOMEINV_DB_MIGRATION_PASSWORD_FILE: "/run/secrets/db-migration-password"
    secrets:
      - db-migration-password
    tmpfs:
      - /tmp
    depends_on:
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m

  bootstrap:
    image: ghcr.io/greluc/home-inv:latest
    container_name: homeinv-bootstrap
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: "no"
    profiles:
      - minimal
      - standard
      - ha
    networks:
      - internal
    environment:
      SPRING_PROFILES_ACTIVE: "bootstrap"
      HOMEINV_DB_URL: "jdbc:postgresql://postgres:5432/homeinv"
      HOMEINV_DB_USER: "homeinv_app"
      HOMEINV_BOOTSTRAP_EMAIL: "${HOMEINV_BOOTSTRAP_EMAIL}"
      HOMEINV_BOOTSTRAP_DISPLAY_NAME: "${HOMEINV_BOOTSTRAP_DISPLAY_NAME}"
      HOMEINV_BOOTSTRAP_LOCALE: "${HOMEINV_BOOTSTRAP_LOCALE}"
      HOMEINV_BOOTSTRAP_TENANT_NAME: "${HOMEINV_BOOTSTRAP_TENANT_NAME}"
      HOMEINV_DB_PASSWORD_FILE: "/run/secrets/db-password"
      HOMEINV_BOOTSTRAP_PASSWORD_FILE: "/run/secrets/bootstrap-password"
    secrets:
      - db-password
      - bootstrap-password
    tmpfs:
      - /tmp
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m

  postgres:
    image: ghcr.io/greluc/home-inv-postgres:latest
    container_name: homeinv-postgres
    user: "70:70"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      - internal
    command: ["postgres", "-c", "wal_level=replica", "-c", "archive_mode=on", "-c", "archive_command=test ! -f /var/lib/homeinv/wal-archive/%f && cp %p /var/lib/homeinv/wal-archive/%f", "-c", "archive_timeout=900"]
    environment:
      POSTGRES_DB: "homeinv"
      POSTGRES_PASSWORD_FILE: "/run/secrets/postgres-superuser-password"
    secrets:
      - postgres-superuser-password
      - db-password
      - db-migration-password
    volumes:
      - pgdata:/var/lib/postgresql/data
      - pgwal:/var/lib/homeinv/wal-archive
    tmpfs:
      - /tmp
      - /var/run/postgresql
    healthcheck:
      test: ["CMD", "pg_isready"]
      interval: 10s
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
        reservations:
          memory: 1g

  valkey:
    image: docker.io/valkey/valkey:8-alpine
    container_name: homeinv-valkey
    user: "999:1000"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      - internal
    secrets:
      - source: valkey-users.acl
        target: /etc/valkey/users.acl
      - valkey-password
    volumes:
      - kvdata:/data
    tmpfs:
      - /tmp
    healthcheck:
      test: ["CMD", "valkey-cli ping"]
      interval: 10s
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 128m

  blobstore:
    image: ghcr.io/greluc/home-inv-blobstore:latest
    container_name: homeinv-blobstore
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      - internal
    secrets:
      - mtls-blobstore
    volumes:
      - blobdata:/var/lib/homeinv/blobs
    tmpfs:
      - /tmp
    healthcheck:
      test: ["CMD", "/homeinv-blobstore --health"]
      interval: 30s
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 128m
        reservations:
          memory: 32m

  clamav:
    image: docker.io/clamav/clamav:stable
    container_name: homeinv-clamav
    user: "100:101"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      - scanner
    secrets:
      - source: clamav-freshclam.conf
        target: /etc/clamav/freshclam.conf
    volumes:
      - cvddata:/var/lib/clamav
    tmpfs:
      - /tmp
      - /run/clamav
      - /var/log/clamav
    healthcheck:
      test: ["CMD", "clamdcheck.sh"]
      interval: 60s
      start_period: 300s
    deploy:
      resources:
        limits:
          memory: 1536m
        reservations:
          memory: 1g

  egress-proxy:
    image: ghcr.io/greluc/home-inv-egress-proxy:latest
    container_name: homeinv-egress-proxy
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      - scanner
      - egress
    tmpfs:
      - /tmp
      - /var/run
    healthcheck:
      test: ["CMD", "/healthcheck"]
      interval: 30s
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 32m
        reservations:
          memory: 16m

  opensearch:
    image: docker.io/opensearchproject/opensearch:3
    container_name: homeinv-opensearch
    user: "1000:1000"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - standard
      - ha
    networks:
      - internal
    environment:
      OPENSEARCH_INITIAL_ADMIN_PASSWORD: "/run/secrets/opensearch-admin-password"
      DISABLE_INSTALL_DEMO_CONFIG: "true"
      bootstrap.memory_lock: "false"
    secrets:
      - source: opensearch-internal_users.yml
        target: /usr/share/opensearch/config/opensearch-security/internal_users.yml
      - source: mtls-search
        target: /usr/share/opensearch/config/certificates
      - opensearch-admin-password
      - search-password
      - mtls-search
    volumes:
      - osdata:/usr/share/opensearch/data
    tmpfs:
      - /tmp
      - /usr/share/opensearch/logs
      - /usr/share/opensearch/config
    healthcheck:
      test: ["CMD", "curl -fsS -k https://localhost:9200/_cluster/health"]
      interval: 20s
      start_period: 120s
    deploy:
      resources:
        limits:
          memory: 2g
        reservations:
          memory: 1536m

  rabbitmq:
    image: docker.io/library/rabbitmq:4-management-alpine
    container_name: homeinv-rabbitmq
    user: "999:999"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      - internal
    environment:
      RABBITMQ_DEFAULT_USER: "homeinv"
      RABBITMQ_DEFAULT_PASS_FILE: "/run/secrets/mq-password"
    secrets:
      - mq-password
    volumes:
      - mqdata:/var/lib/rabbitmq
    tmpfs:
      - /tmp
      - /var/log/rabbitmq
      - /etc/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics -q ping"]
      interval: 20s
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m

networks:
  edge:
  egress:
  internal:
    internal: true
  frontend:
    internal: true
  scanner:
    internal: true

volumes:
  pgdata:
  pgwal:
  osdata:
  mqdata:
  kvdata:
  cvddata:
  blobdata:

secrets:
  valkey-users.acl:
    file: ../secrets/valkey-users.acl
  clamav-freshclam.conf:
    file: ../secrets/clamav-freshclam.conf
  opensearch-internal_users.yml:
    file: ../secrets/opensearch-internal_users.yml
  postgres-superuser-password:
    file: ../secrets/postgres-superuser-password
  db-password:
    file: ../secrets/db-password
  db-migration-password:
    file: ../secrets/db-migration-password
  data-encryption-master-key:
    file: ../secrets/data-encryption-master-key
  jwt-signing-key:
    file: ../secrets/jwt-signing-key
  bootstrap-password:
    file: ../secrets/bootstrap-password
  url-signing-key:
    file: ../secrets/url-signing-key
  valkey-password:
    file: ../secrets/valkey-password
  mq-password:
    file: ../secrets/mq-password
  search-password:
    file: ../secrets/search-password
  mtls-core:
    file: ../secrets/mtls-core
  mtls-blobstore:
    file: ../secrets/mtls-blobstore
  mtls-search:
    file: ../secrets/mtls-search
  opensearch-admin-password:
    file: ../secrets/opensearch-admin-password
