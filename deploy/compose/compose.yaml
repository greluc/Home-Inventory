# GENERATED FROM ../services.yaml — DO NOT EDIT.
#
# Run `python deploy/generate.py` after changing the matrix. CI regenerates
# this file and fails on any difference, so an edit here is lost and noticed
# rather than lost and not (REQ-NFR-063).


name: home-inv

services:
  web:
    image: ghcr.io/greluc/home-inv-web:latest
    container_name: homeinv-web
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      edge: {}
      frontend: {}
    volumes:
      - type: tmpfs
        target: /tmp
        tmpfs:
          mode: 0o1777
      - type: tmpfs
        target: /var/cache/nginx
        tmpfs:
          mode: 0o1777
      - type: tmpfs
        target: /var/run
        tmpfs:
          mode: 0o1777
    ports:
      - "8080:8080"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/healthcheck"]
      interval: 15s
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 64m

  api:
    image: ghcr.io/greluc/home-inv:latest
    container_name: homeinv-api
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      internal:
        aliases:
          - api-internal
      frontend: {}
      plugin-webhook: {}
      plugin-smtp: {}
      plugin-blobstore-s3: {}
      plugin-blobstore-nextcloud: {}
      plugin-oidc: {}
    environment:
      SPRING_PROFILES_ACTIVE: "api"
      SPRING_PROFILES_INCLUDE: "${HOMEINV_PROFILE}"
      HOMEINV_DB_URL: "jdbc:postgresql://postgres:5432/homeinv"
      HOMEINV_DB_USER: "homeinv_app"
      HOMEINV_PLUGINS_FILE: "${HOMEINV_PLUGINS_FILE:-}"
      HOMEINV_TRACING_ENDPOINT: "${HOMEINV_TRACING_ENDPOINT:-}"
      MANAGEMENT_SERVER_PORT: "8090"
      MANAGEMENT_SERVER_ADDRESS: "api-internal"
      HOMEINV_PLUGIN_HOST_PORT: "${HOMEINV_PLUGIN_HOST_PORT:-0}"
      HOMEINV_TRUSTED_PROXIES: "${HOMEINV_TRUSTED_PROXIES}"
      HOMEINV_PUBLIC_BASE_URL: "${HOMEINV_PUBLIC_BASE_URL}"
      HOMEINV_MEDIA_BASE_URL: "${HOMEINV_MEDIA_BASE_URL}"
      HOMEINV_BLOBSTORE_ENDPOINT: "blobstore:8100"
      HOMEINV_BLOBSTORE_FINGERPRINT: "${HOMEINV_BLOBSTORE_FINGERPRINT}"
      HOMEINV_VALKEY_HOST: "valkey"
      HOMEINV_VALKEY_USER: "homeinv"
      HOMEINV_MQ_HOST: "rabbitmq"
      HOMEINV_MQ_USER: "homeinv"
      HOMEINV_SEARCH_USER: "homeinv"
      HOMEINV_SEARCH_ENGINE: "${HOMEINV_SEARCH_ENGINE:-postgresql}"
      HOMEINV_SEARCH_URL: "https://opensearch:9200"
      HOMEINV_SEARCH_FINGERPRINT: "${HOMEINV_SEARCH_FINGERPRINT}"
      HOMEINV_DB_PASSWORD_FILE: "/run/secrets/db-password"
      HOMEINV_DATA_ENCRYPTION_MASTER_KEY_FILE: "/run/secrets/data-encryption-master-key"
      HOMEINV_JWT_SIGNING_KEY_FILE: "/run/secrets/jwt-signing-key"
      HOMEINV_URL_SIGNING_KEY_FILE: "/run/secrets/url-signing-key"
      HOMEINV_CREDENTIAL_KEY_FILE: "/run/secrets/credential-key"
      HOMEINV_VALKEY_PASSWORD_FILE: "/run/secrets/valkey-password"
      HOMEINV_MQ_PASSWORD_FILE: "/run/secrets/mq-password"
      HOMEINV_SEARCH_PASSWORD_FILE: "/run/secrets/search-password"
      HOMEINV_MTLS_CORE_FILE: "/run/secrets/mtls-core"
    secrets:
      - source: api-plugins.yaml
        target: /run/secrets/plugins.yaml
      - db-password
      - data-encryption-master-key
      - jwt-signing-key
      - url-signing-key
      - credential-key
      - valkey-password
      - mq-password
      - search-password
      - mtls-core
    volumes:
      - type: tmpfs
        target: /tmp
        tmpfs:
          mode: 0o1777
    depends_on:
      postgres:
        condition: service_healthy
      valkey:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      bootstrap:
        condition: service_completed_successfully
      blobstore:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/usr/bin/healthcheck"]
      interval: 15s
      start_period: 90s
    deploy:
      resources:
        limits:
          memory: 1536m
        reservations:
          memory: 768m

  worker:
    image: ghcr.io/greluc/home-inv:latest
    container_name: homeinv-worker
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      internal:
        aliases:
          - worker-internal
      scanner: {}
      plugin-webhook: {}
      plugin-smtp: {}
      plugin-blobstore-s3: {}
      plugin-blobstore-nextcloud: {}
      plugin-oidc: {}
    environment:
      SPRING_PROFILES_ACTIVE: "worker"
      SPRING_PROFILES_INCLUDE: "${HOMEINV_PROFILE}"
      HOMEINV_DB_URL: "jdbc:postgresql://postgres:5432/homeinv"
      HOMEINV_DB_USER: "homeinv_app"
      HOMEINV_PLUGINS_FILE: "${HOMEINV_PLUGINS_FILE:-}"
      HOMEINV_TRACING_ENDPOINT: "${HOMEINV_TRACING_ENDPOINT:-}"
      MANAGEMENT_SERVER_PORT: "8090"
      MANAGEMENT_SERVER_ADDRESS: "worker-internal"
      HOMEINV_TRUSTED_PROXIES: "${HOMEINV_TRUSTED_PROXIES}"
      HOMEINV_DB_MIGRATE_ON_START: "false"
      HOMEINV_PUBLIC_BASE_URL: "${HOMEINV_PUBLIC_BASE_URL}"
      HOMEINV_MEDIA_BASE_URL: "${HOMEINV_MEDIA_BASE_URL}"
      HOMEINV_BLOBSTORE_ENDPOINT: "blobstore:8100"
      HOMEINV_BLOBSTORE_FINGERPRINT: "${HOMEINV_BLOBSTORE_FINGERPRINT}"
      HOMEINV_VALKEY_HOST: "valkey"
      HOMEINV_VALKEY_USER: "homeinv"
      HOMEINV_MQ_HOST: "rabbitmq"
      HOMEINV_MQ_USER: "homeinv"
      HOMEINV_SEARCH_USER: "homeinv"
      HOMEINV_SEARCH_ENGINE: "${HOMEINV_SEARCH_ENGINE:-postgresql}"
      HOMEINV_SEARCH_URL: "https://opensearch:9200"
      HOMEINV_SEARCH_FINGERPRINT: "${HOMEINV_SEARCH_FINGERPRINT}"
      HOMEINV_DB_PASSWORD_FILE: "/run/secrets/db-password"
      HOMEINV_DATA_ENCRYPTION_MASTER_KEY_FILE: "/run/secrets/data-encryption-master-key"
      HOMEINV_URL_SIGNING_KEY_FILE: "/run/secrets/url-signing-key"
      HOMEINV_CREDENTIAL_KEY_FILE: "/run/secrets/credential-key"
      HOMEINV_VALKEY_PASSWORD_FILE: "/run/secrets/valkey-password"
      HOMEINV_MQ_PASSWORD_FILE: "/run/secrets/mq-password"
      HOMEINV_SEARCH_PASSWORD_FILE: "/run/secrets/search-password"
      HOMEINV_MTLS_CORE_FILE: "/run/secrets/mtls-core"
    secrets:
      - source: worker-plugins.yaml
        target: /run/secrets/plugins.yaml
      - db-password
      - data-encryption-master-key
      - url-signing-key
      - credential-key
      - valkey-password
      - mq-password
      - search-password
      - mtls-core
    volumes:
      - type: tmpfs
        target: /tmp
        tmpfs:
          mode: 0o1777
    depends_on:
      postgres:
        condition: service_healthy
      valkey:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      clamav:
        condition: service_healthy
      blobstore:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/usr/bin/healthcheck"]
      interval: 15s
      start_period: 90s
    deploy:
      resources:
        limits:
          memory: 1536m
        reservations:
          memory: 512m

  migrate:
    image: ghcr.io/greluc/home-inv:latest
    container_name: homeinv-migrate
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: "no"
    profiles:
      - minimal
      - standard
      - ha
    networks:
      internal: {}
    environment:
      SPRING_PROFILES_ACTIVE: "migrate"
      HOMEINV_DB_MIGRATE_ON_START: "true"
      HOMEINV_DB_URL: "jdbc:postgresql://postgres:5432/homeinv"
      HOMEINV_DB_MIGRATION_USER: "homeinv_migrator"
      HOMEINV_DB_MIGRATION_PASSWORD_FILE: "/run/secrets/db-migration-password"
    secrets:
      - db-migration-password
    volumes:
      - type: tmpfs
        target: /tmp
        tmpfs:
          mode: 0o1777
    depends_on:
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m

  bootstrap:
    image: ghcr.io/greluc/home-inv:latest
    container_name: homeinv-bootstrap
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: "no"
    profiles:
      - minimal
      - standard
      - ha
    networks:
      internal: {}
    environment:
      SPRING_PROFILES_ACTIVE: "bootstrap"
      HOMEINV_DB_URL: "jdbc:postgresql://postgres:5432/homeinv"
      HOMEINV_DB_USER: "homeinv_app"
      HOMEINV_BOOTSTRAP_EMAIL: "${HOMEINV_BOOTSTRAP_EMAIL}"
      HOMEINV_BOOTSTRAP_DISPLAY_NAME: "${HOMEINV_BOOTSTRAP_DISPLAY_NAME}"
      HOMEINV_BOOTSTRAP_LOCALE: "${HOMEINV_BOOTSTRAP_LOCALE}"
      HOMEINV_BOOTSTRAP_TENANT_NAME: "${HOMEINV_BOOTSTRAP_TENANT_NAME}"
      HOMEINV_DB_PASSWORD_FILE: "/run/secrets/db-password"
      HOMEINV_BOOTSTRAP_PASSWORD_FILE: "/run/secrets/bootstrap-password"
    secrets:
      - db-password
      - bootstrap-password
    volumes:
      - type: tmpfs
        target: /tmp
        tmpfs:
          mode: 0o1777
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m

  postgres:
    image: ghcr.io/greluc/home-inv-postgres:latest
    container_name: homeinv-postgres
    user: "70:70"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      internal: {}
    command: ["postgres", "-c", "wal_level=replica", "-c", "archive_mode=on", "-c", "archive_command=test ! -f /var/lib/homeinv/wal-archive/%f && cp %p /var/lib/homeinv/wal-archive/%f", "-c", "archive_timeout=900"]
    environment:
      POSTGRES_DB: "homeinv"
      POSTGRES_PASSWORD_FILE: "/run/secrets/postgres-superuser-password"
    secrets:
      - postgres-superuser-password
      - db-password
      - db-migration-password
    volumes:
      - pgdata:/var/lib/postgresql
      - pgwal:/var/lib/homeinv/wal-archive
      - type: tmpfs
        target: /tmp
        tmpfs:
          mode: 0o1777
      - type: tmpfs
        target: /var/run/postgresql
        tmpfs:
          mode: 0o1777
    healthcheck:
      test: ["CMD", "pg_isready"]
      interval: 10s
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
        reservations:
          memory: 1g

  valkey:
    image: docker.io/valkey/valkey@sha256:a0dbf4c1d5708782907c10e2c72deff317518518b5288a58416981d9db95d30b
    container_name: homeinv-valkey
    user: "999:1000"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      internal: {}
    command: ["valkey-server", "--aclfile", "/etc/valkey/users.acl", "--appendonly", "yes"]
    secrets:
      - source: valkey-users.acl
        target: /etc/valkey/users.acl
      - valkey-password
    volumes:
      - kvdata:/data
      - type: tmpfs
        target: /tmp
        tmpfs:
          mode: 0o1777
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 10s
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 128m

  blobstore:
    image: ghcr.io/greluc/home-inv-blobstore:latest
    container_name: homeinv-blobstore
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      internal: {}
    secrets:
      - mtls-blobstore
    volumes:
      - blobdata:/var/lib/homeinv/blobs
      - type: tmpfs
        target: /tmp
        tmpfs:
          mode: 0o1777
    healthcheck:
      test: ["CMD", "/homeinv-blobstore", "--health"]
      interval: 30s
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 128m
        reservations:
          memory: 32m

  clamav:
    image: docker.io/clamav/clamav@sha256:1fdfd24c6f0a0fb60788481487459a6d4eda8a9b448641594e04db8410d34422
    container_name: homeinv-clamav
    user: "100:101"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      scanner: {}
    secrets:
      - source: clamav-freshclam.conf
        target: /etc/clamav/freshclam.conf
    volumes:
      - cvddata:/var/lib/clamav
      - type: tmpfs
        target: /tmp
        tmpfs:
          mode: 0o1777
      - type: tmpfs
        target: /run/clamav
        tmpfs:
          mode: 0o1777
      - type: tmpfs
        target: /var/lock
        tmpfs:
          mode: 0o1777
      - type: tmpfs
        target: /var/log/clamav
        tmpfs:
          mode: 0o1777
    healthcheck:
      test: ["CMD", "clamdcheck.sh"]
      interval: 60s
      start_period: 300s
    deploy:
      resources:
        limits:
          memory: 1536m
        reservations:
          memory: 1g

  egress-proxy:
    image: ghcr.io/greluc/home-inv-egress-proxy:latest
    container_name: homeinv-egress-proxy
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      scanner: {}
      egress: {}
      plugin-webhook: {}
      plugin-smtp: {}
      plugin-blobstore-s3: {}
      plugin-blobstore-nextcloud: {}
      plugin-oidc: {}
    secrets:
      - source: egress-proxy-egress-allowlist.conf
        target: /etc/homeinv/egress-allowlist.conf
    volumes:
      - type: tmpfs
        target: /tmp
        tmpfs:
          mode: 0o1777
      - type: tmpfs
        target: /var/run
        tmpfs:
          mode: 0o1777
    healthcheck:
      test: ["CMD", "/homeinv-egress-proxy", "--health"]
      interval: 30s
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 32m
        reservations:
          memory: 16m

  opensearch:
    image: docker.io/opensearchproject/opensearch@sha256:bcc1797519726ceb6d651d4a3e60b7c30da91793914a8dfe75fd441d4f641509
    container_name: homeinv-opensearch
    user: "1000:1000"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - standard
      - ha
    networks:
      internal: {}
    environment:
      OPENSEARCH_INITIAL_ADMIN_PASSWORD: "/run/secrets/opensearch-admin-password"
      DISABLE_INSTALL_DEMO_CONFIG: "true"
      bootstrap.memory_lock: "false"
    secrets:
      - source: opensearch-internal_users.yml
        target: /usr/share/opensearch/config/opensearch-security/internal_users.yml
      - source: mtls-search
        target: /usr/share/opensearch/config/certificates
      - opensearch-admin-password
      - search-password
      - mtls-search
    volumes:
      - osdata:/usr/share/opensearch/data
      - type: tmpfs
        target: /tmp
        tmpfs:
          mode: 0o1777
      - type: tmpfs
        target: /usr/share/opensearch/logs
        tmpfs:
          mode: 0o1777
      - type: tmpfs
        target: /usr/share/opensearch/config
        tmpfs:
          mode: 0o1777
    healthcheck:
      test: ["CMD", "curl", "-fsS", "-k", "https://localhost:9200/_cluster/health"]
      interval: 20s
      start_period: 120s
    deploy:
      resources:
        limits:
          memory: 2g
        reservations:
          memory: 1536m

  rabbitmq:
    image: docker.io/library/rabbitmq@sha256:b3b8b7f95f5382a19f9ea33540e604f30aad081d37ad9aba72255135765373a1
    container_name: homeinv-rabbitmq
    user: "999:999"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - minimal
      - standard
      - ha
    networks:
      internal: {}
    secrets:
      - source: rabbitmq-rabbitmq.conf
        target: /etc/rabbitmq/rabbitmq.conf
      - mq-password
    volumes:
      - mqdata:/var/lib/rabbitmq
      - type: tmpfs
        target: /tmp
        tmpfs:
          mode: 0o1777
      - type: tmpfs
        target: /var/log/rabbitmq
        tmpfs:
          mode: 0o1777
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 20s
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m

  plugin-webhook:
    image: ghcr.io/greluc/home-inv-plugin-webhook:latest
    container_name: homeinv-plugin-webhook
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - standard
      - ha
    networks:
      plugin-webhook: {}
    environment:
      HOMEINV_PLUGIN_PORT: "8200"
      HOMEINV_MTLS_PLUGIN_FILE: "/run/secrets/mtls-plugin-webhook"
      HOMEINV_EGRESS_PROXY: "egress-proxy:8118"
      HOMEINV_WEBHOOK_TIMEOUT_SECONDS: "10"
    secrets:
      - mtls-plugin-webhook
    deploy:
      resources:
        limits:
          memory: 128m
        reservations:
          memory: 32m

  plugin-smtp:
    image: ghcr.io/greluc/home-inv-plugin-smtp:latest
    container_name: homeinv-plugin-smtp
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - standard
      - ha
    networks:
      plugin-smtp: {}
    environment:
      HOMEINV_PLUGIN_PORT: "8201"
      HOMEINV_MTLS_PLUGIN_FILE: "/run/secrets/mtls-plugin-smtp"
      HOMEINV_EGRESS_PROXY: "egress-proxy:8118"
      HOMEINV_SMTP_HOST: "${HOMEINV_SMTP_HOST:-mail.example.org}"
      HOMEINV_SMTP_PORT: "${HOMEINV_SMTP_PORT:-587}"
      HOMEINV_SMTP_SECURITY: "${HOMEINV_SMTP_SECURITY:-starttls}"
      HOMEINV_SMTP_USER: "${HOMEINV_SMTP_USER:-}"
      HOMEINV_SMTP_SENDER: "${HOMEINV_SMTP_SENDER:-}"
      HOMEINV_SMTP_TIMEOUT_SECONDS: "30"
    secrets:
      - mtls-plugin-smtp
      - plugin-smtp-password
    deploy:
      resources:
        limits:
          memory: 128m
        reservations:
          memory: 32m

  plugin-blobstore-s3:
    image: ghcr.io/greluc/home-inv-plugin-blobstore-s3:latest
    container_name: homeinv-plugin-blobstore-s3
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - standard
      - ha
    networks:
      plugin-blobstore-s3: {}
    environment:
      HOMEINV_PLUGIN_PORT: "8202"
      HOMEINV_MTLS_PLUGIN_FILE: "/run/secrets/mtls-plugin-blobstore-s3"
      HOMEINV_EGRESS_PROXY: "egress-proxy:8118"
      HOMEINV_S3_ENDPOINT: "${HOMEINV_S3_ENDPOINT:-}"
      HOMEINV_S3_BUCKET: "${HOMEINV_S3_BUCKET:-}"
      HOMEINV_S3_REGION: "${HOMEINV_S3_REGION:-us-east-1}"
      HOMEINV_S3_PREFIX: "${HOMEINV_S3_PREFIX:-}"
      HOMEINV_S3_ADDRESSING: "${HOMEINV_S3_ADDRESSING:-path}"
      HOMEINV_S3_ACCESS_KEY_ID: "${HOMEINV_S3_ACCESS_KEY_ID:-}"
      HOMEINV_S3_TIMEOUT_SECONDS: "60"
    secrets:
      - mtls-plugin-blobstore-s3
      - plugin-blobstore-s3-secret-key
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 64m

  plugin-blobstore-nextcloud:
    image: ghcr.io/greluc/home-inv-plugin-blobstore-nextcloud:latest
    container_name: homeinv-plugin-blobstore-nextcloud
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - standard
      - ha
    networks:
      plugin-blobstore-nextcloud: {}
    environment:
      HOMEINV_PLUGIN_PORT: "8203"
      HOMEINV_MTLS_PLUGIN_FILE: "/run/secrets/mtls-plugin-blobstore-nextcloud"
      HOMEINV_EGRESS_PROXY: "egress-proxy:8118"
      HOMEINV_NEXTCLOUD_URL: "${HOMEINV_NEXTCLOUD_URL:-}"
      HOMEINV_NEXTCLOUD_USER: "${HOMEINV_NEXTCLOUD_USER:-}"
      HOMEINV_NEXTCLOUD_FOLDER: "${HOMEINV_NEXTCLOUD_FOLDER:-}"
      HOMEINV_NEXTCLOUD_TIMEOUT_SECONDS: "60"
    secrets:
      - mtls-plugin-blobstore-nextcloud
      - plugin-blobstore-nextcloud-password
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 64m

  plugin-oidc:
    image: ghcr.io/greluc/home-inv-plugin-oidc:latest
    container_name: homeinv-plugin-oidc
    user: "10001:10001"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: on-failure
    profiles:
      - standard
      - ha
    networks:
      plugin-oidc: {}
    environment:
      HOMEINV_PLUGIN_PORT: "8204"
      HOMEINV_MTLS_PLUGIN_FILE: "/run/secrets/mtls-plugin-oidc"
      HOMEINV_EGRESS_PROXY: "egress-proxy:8118"
      HOMEINV_OIDC_ISSUER: "${HOMEINV_OIDC_ISSUER:-}"
      HOMEINV_OIDC_CLIENT_ID: "${HOMEINV_OIDC_CLIENT_ID:-}"
      HOMEINV_OIDC_PROVIDER_KEY: "${HOMEINV_OIDC_PROVIDER_KEY:-oidc}"
      HOMEINV_OIDC_DISPLAY_NAME: "${HOMEINV_OIDC_DISPLAY_NAME:-Single sign-on}"
      HOMEINV_OIDC_SCOPES: "${HOMEINV_OIDC_SCOPES:-openid email profile}"
      HOMEINV_OIDC_TIMEOUT_SECONDS: "20"
    secrets:
      - mtls-plugin-oidc
      - plugin-oidc-client-secret
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 192m

networks:
  plugin-webhook:
    internal: true
    ipam:
      config:
        - subnet: 10.89.31.0/24
  plugin-smtp:
    internal: true
    ipam:
      config:
        - subnet: 10.89.32.0/24
  plugin-blobstore-s3:
    internal: true
    ipam:
      config:
        - subnet: 10.89.33.0/24
  plugin-blobstore-nextcloud:
    internal: true
    ipam:
      config:
        - subnet: 10.89.34.0/24
  plugin-oidc:
    internal: true
    ipam:
      config:
        - subnet: 10.89.35.0/24
  edge:
  egress:
  internal:
    internal: true
  frontend:
    internal: true
  scanner:
    internal: true

volumes:
  pgdata:
  pgwal:
  osdata:
  mqdata:
  kvdata:
  cvddata:
  blobdata:

secrets:
  api-plugins.yaml:
    file: ../secrets/api-plugins.yaml
  worker-plugins.yaml:
    file: ../secrets/worker-plugins.yaml
  valkey-users.acl:
    file: ../secrets/valkey-users.acl
  clamav-freshclam.conf:
    file: ../secrets/clamav-freshclam.conf
  egress-proxy-egress-allowlist.conf:
    file: ../secrets/egress-proxy-egress-allowlist.conf
  opensearch-internal_users.yml:
    file: ../secrets/opensearch-internal_users.yml
  rabbitmq-rabbitmq.conf:
    file: ../secrets/rabbitmq-rabbitmq.conf
  postgres-superuser-password:
    file: ../secrets/postgres-superuser-password
  db-password:
    file: ../secrets/db-password
  db-migration-password:
    file: ../secrets/db-migration-password
  data-encryption-master-key:
    file: ../secrets/data-encryption-master-key
  jwt-signing-key:
    file: ../secrets/jwt-signing-key
  bootstrap-password:
    file: ../secrets/bootstrap-password
  credential-key:
    file: ../secrets/credential-key
  url-signing-key:
    file: ../secrets/url-signing-key
  valkey-password:
    file: ../secrets/valkey-password
  mq-password:
    file: ../secrets/mq-password
  search-password:
    file: ../secrets/search-password
  mtls-core:
    file: ../secrets/mtls-core
  mtls-blobstore:
    file: ../secrets/mtls-blobstore
  mtls-plugin-webhook:
    file: ../secrets/mtls-plugin-webhook
  mtls-plugin-oidc:
    file: ../secrets/mtls-plugin-oidc
  plugin-oidc-client-secret:
    file: ../secrets/plugin-oidc-client-secret
  mtls-plugin-blobstore-nextcloud:
    file: ../secrets/mtls-plugin-blobstore-nextcloud
  plugin-blobstore-nextcloud-password:
    file: ../secrets/plugin-blobstore-nextcloud-password
  mtls-plugin-blobstore-s3:
    file: ../secrets/mtls-plugin-blobstore-s3
  plugin-blobstore-s3-secret-key:
    file: ../secrets/plugin-blobstore-s3-secret-key
  mtls-plugin-smtp:
    file: ../secrets/mtls-plugin-smtp
  cosign-plugin-webhook:
    file: ../secrets/cosign-plugin-webhook
  cosign-plugin-smtp:
    file: ../secrets/cosign-plugin-smtp
  cosign-plugin-blobstore-s3:
    file: ../secrets/cosign-plugin-blobstore-s3
  cosign-plugin-blobstore-nextcloud:
    file: ../secrets/cosign-plugin-blobstore-nextcloud
  cosign-plugin-oidc:
    file: ../secrets/cosign-plugin-oidc
  plugin-smtp-password:
    file: ../secrets/plugin-smtp-password
  mtls-search:
    file: ../secrets/mtls-search
  opensearch-admin-password:
    file: ../secrets/opensearch-admin-password
