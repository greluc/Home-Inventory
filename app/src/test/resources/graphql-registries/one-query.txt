# A registry of exactly one query, for GraphQlGuardIT.
#
# NOT generated by tools/persisted_queries.py and deliberately not the file it
# writes: the shipped list is empty until `web/` sends its first GraphQL
# document, and a test that needed it to be non-empty would be a test waiting
# on a client nobody has written yet.
#
# The hash is of: { items(first: 1) { nodes { id } } }
58bffbd1a1ad694117821bb32b96c62ebc07ce917359b93f3fa5d150bbdacd6e  test/one-query.graphql
