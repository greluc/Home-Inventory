`boolean` fields and multi-select rows. Whole label is the hit area and is at least 44px tall on touch.

```jsx
<Checkbox label="Garantie verlängert" hint="Verlängerung um 24 Monate" />
<Checkbox label="Alle 1 284 Artikel" indeterminate />
```
