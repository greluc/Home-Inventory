The `secret` field type. Dashed well + key icon + the words "Wert verborgen" — never dots.

```jsx
<SecretInput onReveal={promptSecondFactor} />
<SecretInput revealed value="XKCD-4417-9920-QQ31" />
```
