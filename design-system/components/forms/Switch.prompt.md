Immediate settings only. For a `boolean` field inside a form use Checkbox — a switch implies the change is already saved.

```jsx
<Switch label="Fortlaufendes Scannen" checked onLabel="An" offLabel="Aus" />
```
