Filterable single-select for long enums. Match highlighting uses the accent tint, which is the one place accent appears outside an action.

```jsx
<ComboBox id="hersteller" options={[{value:"bosch",label:"Bosch",meta:"142 Artikel"}]} />
```
