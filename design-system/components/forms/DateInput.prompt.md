date / datetime on the native control. `color-scheme: dark` makes the picker and its caret follow the theme without any styling of ours.

```jsx
<DateInput type="date" value="2028-04-30" />
<DateInput type="datetime" readOnly value="2026-09-11T09:14" />
```
