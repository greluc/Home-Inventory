Amount plus currency as one control. Right-aligned tabular figures; the currency sits in an attached, quieter cell so it never reads as a second field.

```jsx
<MoneyInput amount={249.9} currency="EUR" />
<MoneyInput amount={249.9} currency="EUR" readOnly />
```
