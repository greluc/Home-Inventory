/** Initials in a circle, or a device glyph in a square. There are no photographs of people in this
 *  product — the only pictures are the user's own photos of their own belongings. */
export interface AvatarProps { name?: string; size?: "sm" | "lg"; device?: boolean; icon?: string }
export declare function Avatar(props: AvatarProps): JSX.Element;
