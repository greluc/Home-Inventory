Views of one object. Three signals for selected.

```jsx
<Tabs value="table" onChange={setView} tabs={[{key:"table",label:"Tabelle",icon:"table"},{key:"grid",label:"Raster",icon:"layout-grid"}]} />
```
