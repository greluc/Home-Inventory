A blocking decision. Focus traps on open, returns to the trigger on close, Esc closes unless there is unsaved input.

```jsx
<Modal title="Artikel endgültig löschen?" footer={<><Button variant="ghost">Abbrechen</Button><Button variant="danger" icon="trash">Löschen</Button></>}>…</Modal>
```
