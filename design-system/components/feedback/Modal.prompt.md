A blocking decision. Focus traps on open, returns to the trigger on close, Esc closes unless there is unsaved input.

```jsx
<Modal title="Delete this item permanently?" footer={<><Button variant="ghost">Cancel</Button><Button variant="danger" icon="trash">Delete</Button></>}>…</Modal>
```
