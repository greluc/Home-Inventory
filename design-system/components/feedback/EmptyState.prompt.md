Empty, no-results, error and first-run all use this. Only the words and the icon change.

```jsx
<EmptyState icon="package" title="No items in this location yet" action={<Button variant="primary" icon="scan-line">Scan a label</Button>}>
  Print a sheet of labels and scan them in — three interactions per item.
</EmptyState>
```
